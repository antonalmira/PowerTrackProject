This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
.gitignore
.repomixignore
archives_and_exports/Database_Backups/powertrack_backup_2026_01_Jan.db
components/tab_analytics.py
components/tab_matrix.py
components/tab_students.py
data_manager.py
database.py
design.ui
dialog_email_templates.ui
dialog_mapping.ui
dialog_progress_logic.ui
main.py
main.spec
master_lists/Names Database-FAE.csv
master_lists/Names Database-Sales.csv
monthly_raw_data/2026_01_Jan/1700-v-scale-idriver-introduction-students.xlsx
monthly_raw_data/2026_01_Jan/900-v-innoswitch-ep-fae-students.xlsx
monthly_raw_data/2026_01_Jan/900-v-innoswitch-ep-sales-students.xlsx
monthly_raw_data/2026_01_Jan/900-v-product-families-fae-students.xlsx
monthly_raw_data/2026_01_Jan/900-v-product-families-sales-students.xlsx
monthly_raw_data/2026_01_Jan/automotive-live-training-students.xlsx
monthly_raw_data/2026_01_Jan/bridgeswitch-for-single-phase-motors-students.xlsx
monthly_raw_data/2026_01_Jan/creepage-and-clearance-fae-students.xlsx
monthly_raw_data/2026_01_Jan/innoswitch-family-training-fae-students.xlsx
monthly_raw_data/2026_01_Jan/innoswitch-family-training-sales-students.xlsx
monthly_raw_data/2026_01_Jan/innoswitch3-part-numbering-students.xlsx
monthly_raw_data/2026_01_Jan/innoswitch3-pd-live-training-fae-students.xlsx
monthly_raw_data/2026_01_Jan/introducing-bridgeswitch-fae-students.xlsx
monthly_raw_data/2026_01_Jan/introducing-bridgeswitch-sales-students.xlsx
monthly_raw_data/2026_01_Jan/introducing-hiperpfs-4-fae-students.xlsx
monthly_raw_data/2026_01_Jan/introducing-hiperpfs-4-sales-students.xlsx
monthly_raw_data/2026_01_Jan/introducing-innoswitch4-and-clampzero-fae-students.xlsx
monthly_raw_data/2026_01_Jan/introducing-innoswitch5-pro-sales-students.xlsx
monthly_raw_data/2026_01_Jan/introducing-linkswitch-3-fae-students.xlsx
monthly_raw_data/2026_01_Jan/introducing-linkswitch-3-sales-students.xlsx
monthly_raw_data/2026_01_Jan/introducing-linkswitch-xt2-fae-students.xlsx
monthly_raw_data/2026_01_Jan/introducing-linkswitch-xt2-sales-students.xlsx
monthly_raw_data/2026_01_Jan/introducing-topswitch-jx-and-hx-fae-students.xlsx
monthly_raw_data/2026_01_Jan/introducing-topswitch-jx-and-hx-sales-students.xlsx
monthly_raw_data/2026_01_Jan/introduction-to-automotive-sales-students.xlsx
monthly_raw_data/2026_01_Jan/introduction-to-bridgeswitch-2-fae-students.xlsx
monthly_raw_data/2026_01_Jan/introduction-to-bridgeswitch-2-sales-students.xlsx
monthly_raw_data/2026_01_Jan/introduction-to-hiperlcs-2-fae-part-1-students.xlsx
monthly_raw_data/2026_01_Jan/introduction-to-hiperlcs-2-sales-students.xlsx
monthly_raw_data/2026_01_Jan/introduction-to-hiperpfs-5-fae-part-1-students.xlsx
monthly_raw_data/2026_01_Jan/introduction-to-hiperpfs-5-sales-part-1-students.xlsx
monthly_raw_data/2026_01_Jan/introduction-to-i2c-a-foundation-course-for-innoswitch-pro-fae-students.xlsx
monthly_raw_data/2026_01_Jan/introduction-to-innomux-2-fae-students.xlsx
monthly_raw_data/2026_01_Jan/introduction-to-innomux-2-sales-students.xlsx
monthly_raw_data/2026_01_Jan/introduction-to-innoswitch3-fae-students.xlsx
monthly_raw_data/2026_01_Jan/introduction-to-innoswitch3-pd-sales-students.xlsx
monthly_raw_data/2026_01_Jan/introduction-to-innoswitch3-pro-fae-students.xlsx
monthly_raw_data/2026_01_Jan/introduction-to-innoswitch3-pro-sales-students.xlsx
monthly_raw_data/2026_01_Jan/introduction-to-innoswitch3-sales-students.xlsx
monthly_raw_data/2026_01_Jan/introduction-to-innoswitch3-tn-fae-students.xlsx
monthly_raw_data/2026_01_Jan/introduction-to-innoswitch3-tn-sales-students.xlsx
monthly_raw_data/2026_01_Jan/introduction-to-innoswitch4-cz-and-clampzero-sales-students.xlsx
monthly_raw_data/2026_01_Jan/introduction-to-innoswitch4-pro-fae-students.xlsx
monthly_raw_data/2026_01_Jan/introduction-to-innoswitch4-pro-sales-students.xlsx
monthly_raw_data/2026_01_Jan/introduction-to-linkswitch-hp-fae-students.xlsx
monthly_raw_data/2026_01_Jan/introduction-to-linkswitch-hp-sales-students.xlsx
monthly_raw_data/2026_01_Jan/introduction-to-linkswitch-tn2-fae-students.xlsx
monthly_raw_data/2026_01_Jan/introduction-to-linkswitch-tn2-sales-students.xlsx
monthly_raw_data/2026_01_Jan/introduction-to-linkswitch-tn2-size-8-and-9-sales-level-course-students.xlsx
monthly_raw_data/2026_01_Jan/introduction-to-linkswitch-xt2sr-sales-students.xlsx
monthly_raw_data/2026_01_Jan/introduction-to-llc-conversion-students.xlsx
monthly_raw_data/2026_01_Jan/introduction-to-lytswitch-6-fae-students.xlsx
monthly_raw_data/2026_01_Jan/introduction-to-lytswitch-6-sales-students.xlsx
monthly_raw_data/2026_01_Jan/introduction-to-mine-cap-fae-students.xlsx
monthly_raw_data/2026_01_Jan/introduction-to-mine-cap-sales-students.xlsx
monthly_raw_data/2026_01_Jan/introduction-to-pi-motor-expert-students.xlsx
monthly_raw_data/2026_01_Jan/introduction-to-scale-ev-fae-students.xlsx
monthly_raw_data/2026_01_Jan/introduction-to-scale-ev-sales-students.xlsx
monthly_raw_data/2026_01_Jan/introduction-to-selling-into-automotive-ev-applications-fae-students.xlsx
monthly_raw_data/2026_01_Jan/introduction-to-tinyswitch-4-fae-students.xlsx
monthly_raw_data/2026_01_Jan/introduction-to-tinyswitch-4-sales-students.xlsx
monthly_raw_data/2026_01_Jan/introduction-to-tinyswitch-5-fae-students.xlsx
monthly_raw_data/2026_01_Jan/introduction-to-tinyswitch-5-sales-students.xlsx
monthly_raw_data/2026_01_Jan/introduction-to-usb-pd-30-standards-students.xlsx
monthly_raw_data/2026_01_Jan/linkswitch-tn2-size-8-and-9-more-power-in-buck-applications-students.xlsx
monthly_raw_data/2026_01_Jan/lytswitch-5-training-fae-students.xlsx
monthly_raw_data/2026_01_Jan/lytswitch-5-training-sales-students.xlsx
monthly_raw_data/2026_01_Jan/scale-idriver-introduction-students.xlsx
monthly_raw_data/2026_01_Jan/students.xlsx
monthly_raw_data/2026_01_Jan/synchronous-rectification-for-high-efficiency-flyback-students.xlsx
PowerTrack.spec
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="main.spec">
# -*- mode: python ; coding: utf-8 -*-


a = Analysis(
    ['main.py'],
    pathex=[],
    binaries=[],
    datas=[],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
    optimize=0,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    [],
    exclude_binaries=True,
    name='main',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    console=True,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
)
coll = COLLECT(
    exe,
    a.binaries,
    a.datas,
    strip=False,
    upx=True,
    upx_exclude=[],
    name='main',
)
</file>

<file path="master_lists/Names Database-FAE.csv">
Full Name,Sign In,Email,Location,Title,Manager
,,,,,
,,,,,
,,,,,
Spencer Chen,12/11/2019,wen-shanspencer.chen@power.com,PI Taiwan - Taipei,AE,"Kenny, Wu"
Shun-Cheng(Romeo) Fan,6/28/2019,shun-chengromeo.fan@power.com,PI Taiwan - Taipei,Senior FAE,"Kenny, Wu"
Jeffrey Lin,4/26/2016,jeffrey.lin@power.com,PI Taiwan - Taipei,Senior FAE,"Kenny, Wu"
Chin-Wei(Jervis) Liu,4/27/2016,chin-weijervis.liu@power.com,PI Taiwan - Taipei,Senior FAE,"Kenny, Wu"
Benny Tsai,5/29/2020,benny.tsai@power.com,PI Taiwan - Taipei,FAE,"Kenny, Wu"
Brent Tung,10/17/2016,brent.tung@power.com,PI Taiwan - Taipei,FAE,"Kenny, Wu"
Jen-Wen(JW) Wang,10/18/2016,jen-wenjw.wang@power.com,PI Taiwan - Taipei,Senior FAE,"Kenny, Wu"
Kenny Wu,4/26/2016,kenny.wu@power.com,PI Taiwan - Taipei,FAE Manager,"Winnie, Cao"
Sui Tsung(Jacky) Wu,7/1/2019,suitsungjacky.wu@power.com,PI Taiwan - Taipei,Senior FAE,"Kenny, Wu"
Tony Fu,3/29/2016,tony.fu@power.com,PI China - Foshan,Senior FAE,"Peng, Wang"
Halbert Han,9/9/2025,halbert.han@power.com,PI China - Beijing,FAE,"Jerry, Xiao"
Nick He,7/26/2022,nick.he@power.com,PI China - Shenzhen,Senior FAE,"Richard, Jiang"
Lakes Hu,9/14/2020,lakes.hu@power.com,PI China - Shenzhen,AE Manager,"EH, Quek"
Mark Hu,10/31/2022,mark.hu@power.com,PI China - Shanghai,Senior FAE,"Anthony, Sun"
Tony Huang,11/25/2019,yulintony.huang@power.com,PI China - Shenzhen,FAE,"Jerry, Xiao"
Frank Jiang,8/15/2022,frank.jiang@power.com,PI China - Shenzhen,Senior FAE,"Tao, Cui"
Richard Jiang,3/5/2021,richard.jiang@power.com,PI China - Shenzhen,FAE Manager,"Peng, Wang"
Charles Li,3/2/2021,charles.li@power.com,PI China - Foshan,Senior FAE,"Richard, Jiang"
Zoro Li,11/2/2021,zoro.li@power.com,PI China - Shenzhen,Senior FAE,"Richard, Jiang"
QingXin Liu,3/8/2016,qingxin.liu@power.com,PI China - Xiamen,Senior FAE,"Jerry, Xiao"
Ant Ma,3/7/2016,ant.ma@power.com,PI China - Chengdu,FAE,"Tao, Cui"
Michael Ma,5/6/2024,michael.ma@power.com,PI China - Shanghai,Senior FAE,"Jerry, Xiao"
Daniel Shan,1/9/2017,daniel.shan@power.com,PI China - Qingdao,Senior FAE,"Peng, Wang"
Jim Su,7/12/2022,jim.su@power.com,PI China - Shenzhen,Senior FAE,"Jerry, Xiao"
XiaoGe Wang,4/27/2021,xiaoge.wang@power.com,PI China - Shanghai,Senior FAE,"Steven, Chang"
Bruce Wu,12/1/2021,bruce.wu@power.com,PI China - Shanghai,FAE,"Felix, Zhu"
Simen Wu,9/2/2020,simen.wu@power.com,PI China - Shenzhen,Senior AE,"EH, Quek"
Edwin Xu,6/2/2023,edwin.xu@power.com,PI China - Qingdao,Senior FAE,"Peng, Wang"
Bill Yi,3/25/2016,zhongbiao.yi@power.com,PI China - Shenzhen,Senior FAE,Peter Pan
Sky Yin,10/19/2021,sky.yin@power.com,PI China - Shenzhen,FAE,"Xiaoge, Wang"
Luccus Yu,9/9/2021,luccus.yu@power.com,PI China - Shenzhen,Senior AE,"Jack, Zhang"
Jack Zhang,9/16/2018,jack.zhang@power.com,PI China - Shenzhen,AE Manager,"EH, Quek"
MingYong Zhang,3/15/2016,mingyong.zhang@power.com,PI China - Xiamen,Senior AE,"EH, Quek"
Will Zhang,5/27/2022,will.zhang@power.com,PI China - Qingdao,Senior FAE,"Felix, Zhu"
Qiu Zhao,3/18/2016,qiu.zhao@power.com,PI China - Chengdu,Senior AE,"EH, Quek"
Kirk Zheng,9/9/2021,kirk.zheng@power.com,PI China - Shenzhen,AE,"EH, Quek"
Felix Zhu,12/24/2024,felix.zhu@power.com,PI China - Shenzhen,FA Manager,"Steven, Chang"
Jayden Zhu,10/26/2021,jayden.zhu@power.com,PI China - Shenzhen,Senior AE,"EH, Quek"
Jianxiang Zou,3/8/2016,jianxiang.zou@power.com,PI China - Xiamen,Senior FAE,Aaron Xu
EH Quek,3/9/2016,eh.quek@power.com,PI China - Shenzhen,Director of China Applications,"Steven, Chang"
Henry Choi,8/16/2018,henry.choi@power.com,PI Korea - Seoul,FAE,"Dumas, Lee"
Martin Han,8/1/2022,martin.han@power.com,PI Korea - Seoul,FAE,"Dumas, Lee"
Albert Ju,10/20/2019,hyun-seokalbert.ju@power.com,PI Korea - Seoul,FAE,"Dumas, Lee"
Sung Hun Kang,8/2/2016,sunghun.kang@power.com,PI Korea - Seoul,Senior FAE ,"Dumas, Lee"
Sean Lee,6/3/2016,sean.lee@power.com,PI Korea - Seoul,FAE,"Dumas, Lee"
Dumas Lee,7/25/2019,dumas.lee@power.com,PI Korea - Seoul,Senior FAE Manager,"Won-Il, Kim"
Hiroyuki Murakami,5/9/2018,hiroyuki.murakami@power.com,PI Japan - Yokohama,FAE,"Akira, Hiraishi"
Yuki Fujioka,11/19/2025,yuki.fujioka@power.com,PI Japan - Yokohama,FAE,"Naoki, Ogawa"
Naoki Ogawa,6/6/2016,naoki.ogawa@power.com,PI Japan - Yokohama,Field Applications Director,"Akira, Hiraishi"
Virgil Roy Ajo,12/11/2019,virgil.ajo@power.com,"PI US - Headquarters - San Jose, CA",FAE,"Igor, Shikh"
Gary Bowers,6/29/2016,gary.bowers@power.com,PI US Sales - Florida,Principal FAE,"Brent, Wistner"
Sherry Feng,4/5/2021,sherry.feng@power.com,PI Canada - Montreal,Senior FAE,"Igor, Shikh"
Tony Li,12/6/2017,tony.li@power.com,"PI US - Headquarters - San Jose, CA",Staff FSE,"Rahul, Joshi"
Dan Nanao,12/20/2022,dan.nanao@power.com,"PI Philippines - Pasig Metro, Manila",Senior FAE,"Igor, Shikh"
Raymark Parocha,7/21/2022,raymark.parocha@power.com,"PI Philippines - Pasig Metro, Manila",FAE,"Igor, Shikh"
Rowel Raspsing,9/2/2022,rowel.rapsing@power.com,"PI US - Detroit, MI",Senior FAE,"Igor, Shikh"
Kelin Zhang,1/7/2025,kelin.zhang@power.com,PI Canada - Ottawa,FAE,"Igor, Shikh"
Igor Shikh,6/8/2016,igor.shikh@power.com,"PI US - Americas North - Needham, MA",FAE Manager,"Brent, Wistner"
Cagri Agca,9/24/2021,cagri.agca@power.com,PI UK Sales Office,FAE,"Jamie, Carter"
Dmitry Antonov,4/13/2021,dmitry.antonov@power.com,PI UK Sales Office,Senior FAE,"Jamie, Carter"
Daniele Iaconi,1/17/2022,daniele.iaconi@power.com,PI Italy - Sesto San Giovanni,FAE,"Roberto, Martinelli"
Guillem Montolio,7/16/2024,guillem.montolio@power.com,PI Germany - Aschheim,FAE,"Nejmeddine, Hamada"
Fredrik Jeppsson,12/3/2021,fredrik.jeppsson@power.com,PI Germany - Aschheim,Senior FAE,"Nejmeddine, Hamada"
Sandip Panchal,6/7/2016,sandip.panchal@power.com,PI India - Mumbai,Senior FAE,"Bhaskar, Thiagarajan"
Surya Bharti,9/12/2025,surya.bharti@power.com,PI India - Bangalore,FAE,"Bhaskar, Thiagarajan"
Kavit Singh Dhiya,3/4/2024,kavit.dhiya@power.com,PI India - New Delhi,FAE,"Bhaskar, Thiagarajan"
Himmat Parmar,6/7/2016,himmat.parmar@power.com,PI India - Bangalore,Engineering Manager,"Bhaskar, Thiagarajan"
Dominador Viray,7/28/2016,dominador.viray@power.com,PI Singapore,Senior FAE,Andres Tan
Harshavardhan Vuyyuru,9/27/2019,harshavardhan.vuyyuru@power.com,PI India - Bangalore,Senior FAE,"Bhaskar, Thiagarajan"
</file>

<file path="master_lists/Names Database-Sales.csv">
Full Name,Sign In,Email,Location,Title,Manager
,,,,,
,,,,,
,,,,,
Ted Chang,9/13/2021,ted.chang@power.com,PI Taiwan - Taipei,Account Manager,"Winnie, Chao"
Amy Kuo,4/28/2016,amy.kuo@power.com,PI Taiwan - Taipei,Senior Account Manager,"Winnie, Chao"
Jeremy Tsai,5/3/2016,jeremy.tsai@power.com,PI Taiwan - Taipei,Distribution Manager,"Tony, Gao"
Winnie Chao,10/19/2016,winnie.chao@power.com,PI Taiwan - Taipei,Director of Sales Taiwan,"Steven, Chang"
Tao Cui,3/8/2016,tao.cui@power.com,PI China - Chengdu,Senior RSM,"Steven, Chang"
Maobi Ding,8/24/2022,maobi.ding@power.com,PI China - Shanghai,Sales Manager,"Anthony, Sun"
Luby Feng,11/1/2024,luby.feng@power.com,PI China - Shenzhen,TSE,"Wenbin, Liu"
Freya Li,4/12/2022,freya.li@power.com,PI China - Beijing,TSE,"Kelvin, Wu"
Lewis Li,7/3/2025,lewis.li@power.com,PI China - Beijing,Sales Manager,"Peng, Wang"
Bruce Lin,1/5/2022,bruce.lin@power.com,PI China - Shenzhen,FSE,"Jerry, Xiao"
Ben Liu,5/26/2023,ben.liu@power.com,PI China - Qingdao,Key Account Manager,"Peng, Wang"
Wenbin Liu,5/6/2021,wenbin.liu@power.com,PI China - Shenzhen,Distribution Manager,"Jerry, Xiao"
Nic Long,5/13/2025,nic.long@power.com,PI China - Shenzhen,TSE,"Jerry, Xiao"
Henry Luo,2/15/2022,henry.luo@power.com,PI China - Shenzhen,Field Sales Manager,"Steven, Chang"
Jason Luo,5/24/2022,jason.luo@power.com,PI China - Beijing,Key Account Manager,"Peng, Wang"
Shawn Qin,8/9/2022,shawn.qin@power.com,PI China - Shanghai,BD Manager,"Michael, Hornkamp"
Richard Song,5/8/2024,richard.song@power.com,PI China - Shenzhen,Distribution Manager,"Jerry, Xiao"
Anthony Sun,7/1/2024,anthony.sun@power.com,PI China - Shanghai,Regional Sales Manager,"Steven, Chang"
Brant Sun,5/23/2022,brant.sun@power.com,PI China - Shanghai,TSE,"Kelvin, Wu"
Jacky Sun,4/28/2016,jacky.sun@power.com,PI China - Shanghai,Field Sales Manager,"Anthony, Sun"
Marko Wang,11/10/2021,marko.wang@power.com,PI China - Shanghai,FSE,"Kelvin, Wu"
Peng Wang,10/31/2019,peng.wang@power.com,PI China - Shenzhen,Director of Sales,"Steven, Chang"
Kelvin Wu,6/23/2021,kelvin.wu@power.com,PI China - Shanghai,Senior Distribution Manager,"Jerry, Xiao"
Long (Aaron) Xu,7/30/2018,longaaron.xu@power.com,PI China - Xiamen,Field Sales Manager,"Steven, Chang"
Lion Yan,3/29/2022,lion.yan@power.com,PI China - Shanghai,Key Account Manager,"Anthony, Sun"
Marshall Ye,11/10/2021,marshall.ye@power.com,PI China - Xiamen,FSE,"Jerry, Xiao"
Hardy Yin,2/1/2021,hardy.yin@power.com,PI China - Shenzhen,Key Account Manager,"Peng, Wang"
Henry Zhu,3/5/2024,henry.zhu@power.com,PI China - Shenzhen,Key Account Manager,"Peng, Wang"
Steven Chang,12/9/2022,steven.chang@power.com,PI China - Shenzhen,Senior director of Sales,"Gagan, Jian"
Cheon Kyu Park,7/7/2017,ckpark@power.com,PI Korea - Seoul,Senior Sales Manager,"Won-Il, Kim"
Joon Ho Kim,3/18/2016,joonho.kim@power.com,PI Korea - Seoul,Senior Sales Manager,"Won-Il, Kim"
Dong Sik (Tommy) Lim,3/18/2016,tommy.lim@power.com,PI Korea - Seoul,Senior Sales Manager,"Won-Il, Kim"
Won-Il Kim,5/10/2017,won-il.kim@power.com,PI Korea - Seoul,Senior Director of Sales Korea,"Gagan, Jian"
Masashi Imahashi,9/5/2021,masashi.imahashi@power.com,PI Japan - Yokohama,Field Sales Manager,"Akira, Hiraishi"
Yoshinori Ikeda,1/7/2025,yoshinori.ikeda@power.com,PI Japan - Yokohama,Senior Field Sales Manager,"Akira, Hiraishi"
Yoshiaki Takahashi,12/2/2024,yoshiaki.takahashi@power.com,PI Japan - Yokohama,Field Sales Manager,"Akira, Hiraishi"
Kyoichi Kanari,2/17/2017,kyoichi.kanari@power.com,PI Japan - Yokohama,Field Sales Manager,"Akira, Hiraishi"
Akira Hiraishi,7/6/2022,akira.hiraishi@power.com,PI Japan - Yokohama,Senior Sales Director,"Gagan, Jian"
Gary Bowers,6/29/2016,gary.bowers@power.com,PI US Sales - Florida,"Sr. Manager, Engineering - High Power","Brent, Wistner"
Yang Luo,10/16/2023,yang.luo@power.com,PI Canada - Montreal,FSE,"Karan, Pande"
Karan Pande,11/20/2020,karan.pande@power.com,PI Canada - Montreal,Channel Sales Engineer,"Ray, Ponkey"
Ray Ponkey,6/29/2018,ray.ponkey@power.com,PI US Sales - Michigan,Sr. Field Sales Manager,"Brent, Wistner"
Ali Rezaee,10/27/2021,ali.rezaee@power.com,PI US Sales - Plymouth,FSE,"Ray, Ponkey"
Jeffrey Reynolds,1/9/2023,jeffrey.reynolds@power.com,"PI US - Americas South - San Jose, CA",FSE,"Ray, Ponkey"
Brent Wistner,10/4/2019,brent.wistner@power.com,"PI US - Americas South - Suwanee, GA",Associate Sales Director,"Gagan, Jian"
Gagan Jain,10/7/2016,gagan.jain@power.com,"PI US - Headquarters - San Jose, CA",Senior Director of Sales,"Gagan, Jian"
Salvatore Capobianco,7/15/2019,salvatore.capobianco@power.com,PI UK Sales Office,Senior Field Sales Manager,"Ben, Sutherland"
Jamie Carter,2/17/2023,jamie.carter@power.com,PI UK Sales Office,Senior Sales Manager,"Ben, Sutherland"
Manoj Chaudhary,7/11/2022,manoj.chaudhary@power.com,PI UK Sales Office,Field Sales Manager,"Eric, Castagne"
Jochen Gerkmann,8/2/2023,jochen.gerkmann@power.com,PI Germany - Aschheim,Field Sales Manager,"Nejmeddine, Hamada"
Cihan Gocmen,Not signed,cihan.gocmen@power.com,PI Germany - Aschheim,TSE,"Jamie, Carter"
Nejmeddine Hamada,5/20/2021,nejmeddine.hamada@power.com,PI Germany - Aschheim,Regional Sales Manager,"Ben, Sutherland"
Cemil Kilerci,1/31/2022,cemil.kilerci@power.com,PI UK Sales Office,FSE,"Jamie, Carter"
Cristian Pena,9/6/2022,cristian.pena@power.com,PI UK Sales Office,FSE,"Jamie, Carter"
Zeynep Toezge,9/17/2024,zeynep.toezge@power.com,PI Germany - Aschheim,TSE,"Jamie, Carter"
Eric Castagne,6/7/2016,eric.castagne@power.com,PI Germany - Ense,Director of Sales,"Ben, Sutherland"
Ben Sutherland,4/17/2017,ben.sutherland@power.com,PI UK (CamSemi),Senior Director Sales,"Gagan, Jian"
Shishir Herle,7/11/2022,shishir.herle@power.com,PI India - Bangalore,Field Sales Manager,"Eric, Castagne"
Himanshu Bhardwaj,4/15/2024,himanshu.bhardwaj@power.com,PI India - Bangalore,FSE,"Abhishek, Rao"
Vijay Dere,10/13/2025,vijay.dere@power.com,PI India - Bangalore,Field Sales Manager,"Swaminathan, Srinivasan"
Adarsh Dharmaraju,Not Available,Adarsh Dharmaraju,PI India - Bangalore,Field Sales Manager,"Swaminathan, Srinivasan"
Abhishek Rao,4/12/2023,abhishek.rao@power.com,PI India - Bangalore,Sales Manager,"Bhaskar, Thiagarajan"
Shashank S,9/5/2022,shashank@power.com,PI India - Bangalore,FSE,"Bhaskar, Thiagarajan"
Vishal Saini,8/20/2024,vishal.saini@power.com,PI India - Bangalore,Field Sales Manager,"Bhaskar, Thiagarajan"
Swaminathan Srinivasan,5/3/2017,swaminathan.srinivasan@power.com,PI India - Bangalore,Senior Field Sales Manager,"Bhaskar, Thiagarajan"
Andreas Tan,7/7/2022,andreas.tan@power.com,PI Singapore,Field Sales Manager,"Tony, Gao"
Bhaskar Thiagarajan,6/7/2016,bhaskar.thiagarajan@power.com,PI India - Bangalore,Associate Director,"Ben, Sutherland"
Ben Sutherland,4/17/2017,ben.sutherland@power.com,PI UK (CamSemi),Senior Director Sales,"Gagan, Jian"
</file>

<file path="PowerTrack.spec">
# -*- mode: python ; coding: utf-8 -*-


a = Analysis(
    ['main.py'],
    pathex=[],
    binaries=[],
    datas=[('design.ui', '.'), ('dialog_email_templates.ui', '.'), ('dialog_mapping.ui', '.'), ('dialog_progress_logic.ui', '.')],
    hiddenimports=['win32com', 'bleach', 'pandas', 'matplotlib'],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
    optimize=0,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    [],
    exclude_binaries=True,
    name='PowerTrack',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    console=False,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
)
coll = COLLECT(
    exe,
    a.binaries,
    a.datas,
    strip=False,
    upx=True,
    upx_exclude=[],
    name='PowerTrack',
)
</file>

<file path=".gitignore">
__pycache__/
*.pyc
.venv/
venv/
.env
.vscode/
.idea/
data/
</file>

<file path=".repomixignore">
# Ignore python cache files
**/__pycache__/
**/*.pyc

# Ignore virtual environments
venv/
.venv/

# Ignore local datasets
data/

powertrack.db

# Ignore previous repomix outputs
repomix-output.xml
</file>

<file path="dialog_email_templates.ui">
<?xml version="1.0" encoding="UTF-8"?>
<ui version="4.0">
 <class>TemplateEditorDialog</class>
 <widget class="QDialog" name="TemplateEditorDialog">
  <property name="geometry">
   <rect><x>0</x><y>0</y><width>600</width><height>500</height></rect>
  </property>
  <property name="windowTitle">
   <string>Edit Email Templates</string>
  </property>
  <property name="styleSheet">
   <string notr="true">
    QDialog { background: #202025; color: white; }
    QLabel { color: white; font-weight: bold; }
    QLineEdit, QTextEdit, QComboBox { background: #121214; color: white; border: 1px solid #3a3a40; border-radius: 4px; padding: 5px; }
    QPushButton { padding: 6px; border-radius: 4px; font-weight: bold; background: #0085ca; color: white; }
    QPushButton:hover { background: #3c649f; }
   </string>
  </property>
  <layout class="QVBoxLayout" name="verticalLayout">
   <item>
    <layout class="QHBoxLayout" name="horizontalLayout">
     <item>
      <widget class="QLabel" name="label">
       <property name="text"><string>Category Rule:</string></property>
      </widget>
     </item>
     <item>
      <widget class="QComboBox" name="combo_category">
       <property name="sizePolicy"><sizepolicy hsizetype="Expanding" vsizetype="Fixed"><horstretch>0</horstretch><verstretch>0</verstretch></sizepolicy></property>
      </widget>
     </item>
    </layout>
   </item>
   <item>
    <widget class="QLabel" name="label_2">
     <property name="text"><string>Email Subject:</string></property>
    </widget>
   </item>
   <item>
    <widget class="QLineEdit" name="edit_subject"/>
   </item>
   <item>
    <widget class="QLabel" name="label_3">
     <property name="text"><string>Email Body (HTML supported):</string></property>
    </widget>
   </item>
   <item>
    <widget class="QTextEdit" name="edit_body"/>
   </item>
   <item>
    <widget class="QDialogButtonBox" name="buttonBox">
     <property name="standardButtons"><set>QDialogButtonBox::Close</set></property>
    </widget>
   </item>
  </layout>
 </widget>
 <resources/>
 <connections>
  <connection>
   <sender>buttonBox</sender>
   <signal>accepted()</signal>
   <receiver>TemplateEditorDialog</receiver>
   <slot>accept()</slot>
  </connection>
  <connection>
   <sender>buttonBox</sender>
   <signal>rejected()</signal>
   <receiver>TemplateEditorDialog</receiver>
   <slot>reject()</slot>
  </connection>
 </connections>
</ui>
</file>

<file path="dialog_mapping.ui">
<?xml version="1.0" encoding="UTF-8"?>
<ui version="4.0">
 <class>CourseMappingDialog</class>
 <widget class="QDialog" name="CourseMappingDialog">
  <property name="geometry">
   <rect><x>0</x><y>0</y><width>1000</width><height>700</height></rect>
  </property>
  <property name="windowTitle">
   <string>Map Courses to Control Codes</string>
  </property>
  <property name="styleSheet">
   <string notr="true">
    QDialog { background: #202025; color: white; }
    QLabel { color: white; font-weight: bold; }
    QTreeWidget, QListWidget { background: #121214; border: 1px solid #3a3a40; color: white; outline: none; border-radius: 4px; padding: 4px;}
    QTreeWidget::item:selected, QListWidget::item:selected { background: #0085ca; }
    QTreeWidget::item:hover, QListWidget::item:hover { background: #2a2a30; }
    QPushButton { padding: 10px; border-radius: 4px; font-weight: bold; }
    QPushButton#btn_save { background: #0085ca; color: white; font-size: 14px;}
    QPushButton#btn_save:hover { background: #3c649f; }
    
    QScrollBar:vertical { background: #121214; width: 14px; margin: 0px 0px 0px 0px; border-left: 1px solid #3a3a40; }
    QScrollBar::handle:vertical { background: #3a3a40; min-height: 20px; border-radius: 7px; margin: 2px; }
    QScrollBar::handle:vertical:hover { background: #555555; }
    QScrollBar::add-line:vertical, QScrollBar::sub-line:vertical { height: 0px; }
    
    QScrollBar:horizontal { background: #121214; height: 14px; margin: 0px 0px 0px 0px; border-top: 1px solid #3a3a40; }
    QScrollBar::handle:horizontal { background: #3a3a40; min-width: 20px; border-radius: 7px; margin: 2px; }
    QScrollBar::handle:horizontal:hover { background: #555555; }
    QScrollBar::add-line:horizontal, QScrollBar::sub-line:horizontal { width: 0px; }
   </string>
  </property>
  <layout class="QVBoxLayout" name="main_layout">
   <item>
    <layout class="QHBoxLayout" name="horizontalLayout">
     
     <!-- LEFT SIDE: ASSIGNED COURSES TREE -->
     <item>
      <layout class="QVBoxLayout" name="left_layout">
       <item>
        <widget class="QLabel" name="lbl_tree">
         <property name="text"><string>Assigned Courses:</string></property>
        </widget>
       </item>
       <item>
        <widget class="QTreeWidget" name="tree_categories">
         <property name="headerHidden"><bool>true</bool></property>
         <property name="dragEnabled"><bool>true</bool></property>
         <property name="acceptDrops"><bool>true</bool></property>
         <property name="dragDropMode"><enum>QAbstractItemView::DragDrop</enum></property>
         <property name="selectionMode"><enum>QAbstractItemView::ExtendedSelection</enum></property>
         <column><property name="text"><string notr="true">1</string></property></column>
        </widget>
       </item>
      </layout>
     </item>

     <!-- RIGHT SIDE: UNASSIGNED COURSES -->
     <item>
      <layout class="QVBoxLayout" name="right_layout">
       <item>
        <widget class="QLabel" name="lbl_list">
         <property name="text"><string>Unassigned Courses:</string></property>
        </widget>
       </item>
       <item>
        <widget class="QListWidget" name="list_unassigned">
         <property name="dragEnabled"><bool>true</bool></property>
         <property name="acceptDrops"><bool>true</bool></property>
         <property name="dragDropMode"><enum>QAbstractItemView::DragDrop</enum></property>
         <property name="selectionMode"><enum>QAbstractItemView::ExtendedSelection</enum></property>
        </widget>
       </item>
      </layout>
     </item>

    </layout>
   </item>
   
   <item>
    <widget class="QPushButton" name="btn_save">
     <property name="text"><string>Save Changes</string></property>
    </widget>
   </item>
  </layout>
 </widget>
 <resources/>
 <connections/>
</ui>
</file>

<file path="dialog_progress_logic.ui">
<?xml version="1.0" encoding="UTF-8"?>
<ui version="4.0">
 <class>ProgressLogicDialog</class>
 <widget class="QDialog" name="ProgressLogicDialog">
  <property name="geometry">
   <rect><x>0</x><y>0</y><width>650</width><height>400</height></rect>
  </property>
  <property name="windowTitle">
   <string>Edit Progress Logic</string>
  </property>
  <property name="styleSheet">
   <string notr="true">
    QDialog { background: #202025; color: white; }
    QLabel { color: white; font-weight: bold; }
    QTableWidget { background: #121214; color: white; border: 1px solid #3a3a40; gridline-color: #3a3a40; }
    QHeaderView::section { background-color: #2a2a30; color: white; font-weight: bold; border: 1px solid #3a3a40; }
    QPushButton { padding: 6px; border-radius: 4px; font-weight: bold; background: #0085ca; color: white; }
    QPushButton:hover { background: #3c649f; }
    QPushButton#btn_delete { background: #f08080; color: #121214; }
    QPushButton#btn_delete:hover { background: #cd5c5c; }
    QPushButton#btn_edit_templates { background: #00B050; color: white; }
    QPushButton#btn_edit_templates:hover { background: #008a3e; }
    QComboBox, QDoubleSpinBox, QLineEdit { background: #1a1a1e; color: white; border: 1px solid #3a3a40; }
   </string>
  </property>
  <layout class="QVBoxLayout" name="verticalLayout">
   <item>
    <widget class="QLabel" name="label">
     <property name="text"><string>Progress Rules (Evaluated Top to Bottom)</string></property>
    </widget>
   </item>
   <item>
    <widget class="QTableWidget" name="table_rules">
     <property name="columnCount"><number>4</number></property>
     <column><property name="text"><string>Category Name</string></property></column>
     <column><property name="text"><string>Metric</string></property></column>
     <column><property name="text"><string>Operator</string></property></column>
     <column><property name="text"><string>Value (%)</string></property></column>
    </widget>
   </item>
   <item>
    <layout class="QHBoxLayout" name="horizontalLayout">
     <item><widget class="QPushButton" name="btn_up"><property name="text"><string>Move Up</string></property></widget></item>
     <item><widget class="QPushButton" name="btn_down"><property name="text"><string>Move Down</string></property></widget></item>
     <item><spacer><property name="orientation"><enum>Qt::Horizontal</enum></property></spacer></item>
     <item><widget class="QPushButton" name="btn_add"><property name="text"><string>+ Add Rule</string></property></widget></item>
     <item><widget class="QPushButton" name="btn_delete"><property name="text"><string>- Delete Rule</string></property></widget></item>
    </layout>
   </item>
   <item>
    <layout class="QHBoxLayout" name="horizontalLayout_2">
     <item><widget class="QPushButton" name="btn_edit_templates"><property name="text"><string>Edit Templates</string></property></widget></item>
     <item><spacer><property name="orientation"><enum>Qt::Horizontal</enum></property></spacer></item>
     <item>
      <widget class="QDialogButtonBox" name="buttonBox">
       <property name="standardButtons"><set>QDialogButtonBox::Cancel|QDialogButtonBox::Save</set></property>
      </widget>
     </item>
    </layout>
   </item>
  </layout>
 </widget>
 <resources/>
 <connections>
  <connection>
   <sender>buttonBox</sender>
   <signal>accepted()</signal>
   <receiver>ProgressLogicDialog</receiver>
   <slot>accept()</slot>
  </connection>
  <connection>
   <sender>buttonBox</sender>
   <signal>rejected()</signal>
   <receiver>ProgressLogicDialog</receiver>
   <slot>reject()</slot>
  </connection>
 </connections>
</ui>
</file>

<file path="data_manager.py">
import csv
import pandas as pd
import difflib
import os
import glob
import re
import sys
from database import get_connection, setup_database


# ── Configurable constants ─────────────────────────────────────────────────────

INTERNAL_DOMAINS = ["@power.com", "@powerint.com"]
FUZZY_CUTOFF = 0.75

# ── Shared utilities ───────────────────────────────────────────────────────────

def normalize_uid(uid) -> str:
    s = str(uid).strip()
    return s[:-2] if s.endswith(".0") else s

def parse_region(location_str: str) -> str:
    loc = str(location_str).lower()

    def _match(words):
        return any(re.search(r"\b" + re.escape(w) + r"\b", loc) for w in words)

    if _match(["india", "sea", "singapore", "malaysia", "isea",
               "vietnam", "thailand", "indonesia", "australia", "new zealand"]):
        return "ISEA"
    if _match(["usa", "canada", "america", "mexico", "brazil", "argentina",
               "san jose", "california", "texas", "boston", "philippines"]):
        return "Americas"
    if re.search(r"\bus\b", loc):
        return "Americas"
    if _match(["europe", "germany", "italy", "uk", "switzerland", "france",
               "munich", "spain", "poland"]):
        return "Europe"
    if _match(["china", "beijing", "shenzhen", "shanghai", "qingdao",
               "chengdu", "foshan", "xiamen"]):
        return "China"
    if _match(["taiwan", "taipei"]):
        return "Taiwan"
    if _match(["japan", "tokyo", "yokohama"]):
        return "Japan"
    if _match(["korea", "seoul"]):
        return "Korea"
    return "Worldwide"

def clean_string_for_matching(text: str) -> str:
    if not isinstance(text, str): return ""
    text = text.lower()
    text = text.replace("-sales-students.xlsx", "").replace("-fae-students.xlsx", "")
    text = text.replace("students.xlsx", "").replace("-students.xlsx", "")
    text = text.replace(".xlsx", "").replace(".csv", "")
    text = text.replace("introduction to", "introducing").replace("introduction-to", "introducing")
    return re.sub(r"[^a-z0-9]", "", text)

def safe_float(val, default=0.0) -> float:
    try:
        return float(str(val).replace("%", "").strip())
    except (ValueError, TypeError):
        return default

def load_dataframe(file_path: str, header_row=0):
    try:
        if header_row == "infer": header_row = 0
        if file_path.lower().endswith(".csv"):
            return pd.read_csv(file_path, low_memory=False, header=header_row)
        elif file_path.lower().endswith((".xlsx", ".xls")):
            return pd.read_excel(file_path, header=header_row)
    except Exception as exc:
        print(f"[ERROR] loading {file_path}: {exc}")
    return None

# ── DataManager ────────────────────────────────────────────────────────────────

class DataManager:
    def __init__(self):
        setup_database()

    # ── Auto-Import Master Lists ───────────────────────────────────────────────
    
    def auto_import_master_lists(self):
        if getattr(sys, 'frozen', False):
            base_dir = os.path.dirname(sys.executable)
        else:
            base_dir = os.path.dirname(os.path.abspath(__file__))
            
        master_dir = os.path.join(base_dir, "master_lists")
        
        if not os.path.exists(master_dir):
            os.makedirs(master_dir)
            return

        for filename in os.listdir(master_dir):
            if filename.lower().endswith(".csv"):
                file_path = os.path.join(master_dir, filename)
                self._process_master_csv(file_path)

    def _process_master_csv(self, file_path: str):
        df = load_dataframe(file_path)
        if df is None: return

        cols = {str(c).lower().strip(): c for c in df.columns}
        email_col = cols.get("email")
        if not email_col:
            print(f"[WARN] No 'Email' column in {file_path}. Skipping.")
            return

        df = df.dropna(subset=[email_col])
        df = df[df[email_col].astype(str).str.contains("@", na=False)]

        org_label = "FAE" if "fae" in file_path.lower() else "Sales" if "sales" in file_path.lower() else "Unknown"

        with get_connection() as conn:
            cursor = conn.cursor()
            for _, row in df.iterrows():
                email = str(row.get(email_col)).strip().lower()
                name = str(row.get(cols.get("full name", "Full Name"), "")).strip()
                loc = str(row.get(cols.get("location", "Location"), "")).strip()
                title = str(row.get(cols.get("title", "Title"), "")).strip()
                mgr = str(row.get(cols.get("manager", "Manager"), "")).strip()
                
                parts = name.split(" ", 1)
                first = parts[0]
                last = parts[1] if len(parts) > 1 else ""
                region = parse_region(loc)

                cursor.execute("SELECT user_id FROM students WHERE LOWER(email)=?", (email,))
                existing = cursor.fetchone()
                
                if existing:
                    cursor.execute("""
                        UPDATE students 
                        SET organization=?, location=?, title=?, manager=?, region_bucket=?, first_name=?, last_name=?
                        WHERE user_id=?
                    """, (org_label, loc, title, mgr, region, first, last, existing[0]))
                else:
                    cursor.execute("""
                        INSERT INTO students (user_id, first_name, last_name, email, organization, location, title, manager, region_bucket)
                        VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?)
                    """, (email, first, last, email, org_label, loc, title, mgr, region))

    # ── Students ───────────────────────────────────────────────────────────────

    def import_students(self, file_path: str):
        df = load_dataframe(file_path)
        if df is None: return

        cols = {str(c).lower().strip(): c for c in df.columns}
        uid_col = cols.get("user id")
        if not uid_col: return

        df = df.dropna(subset=[uid_col])
        records = []
        for _, row in df.iterrows():
            uid = normalize_uid(row.get(uid_col, ""))
            if not uid: continue

            first = str(row.get(cols.get("first name", "First name"), "")).strip()
            last  = str(row.get(cols.get("last name",  "Last name"),  "")).strip()
            email = str(row.get(cols.get("email",      "Email"),      "")).strip()

            if first.lower() == "nan": first = ""
            if last.lower()  == "nan": last  = ""
            if email.lower() == "nan": email = ""

            records.append((uid, first, last, email, str(row.get(cols.get("signed up"), "")), str(row.get(cols.get("latest activity"), ""))))

        with get_connection() as conn:
            cursor = conn.cursor()
            for uid, first, last, email, signed_up, latest_activity in records:
                if email:
                    em_lower = email.lower()
                    cursor.execute("SELECT user_id FROM students WHERE user_id=?", (uid,))
                    if cursor.fetchone():
                        # Real UID already exists. Delete the placeholder email UID to prevent duplicates
                        cursor.execute("DELETE FROM students WHERE user_id=?", (em_lower,))
                    else:
                        # Rename the placeholder to the Real UID so we inherit Master List org data
                        cursor.execute("UPDATE OR IGNORE students SET user_id=? WHERE user_id=?", (uid, em_lower))

                cursor.execute(
                    '''
                    INSERT INTO students (user_id, first_name, last_name, email, signed_up, latest_activity)
                    VALUES (?, ?, ?, ?, ?, ?)
                    ON CONFLICT(user_id) DO UPDATE SET
                        first_name     = excluded.first_name,
                        last_name      = excluded.last_name,
                        email          = excluded.email
                    ''', (uid, first, last, email, signed_up, latest_activity)
                )

    # ── Snapshot Archiving ─────────────────────────────────────────────────────
    
    def create_monthly_snapshot(self, year: int, month: int):
        with get_connection() as conn:
            conn.execute("DELETE FROM completions_history WHERE year=? AND month=?", (year, month))
            conn.execute("""
                INSERT INTO completions_history (user_id, course_id, year, month, percent_complete)
                SELECT user_id, course_id, ?, ?, percent_complete FROM completions
            """, (year, month))

    # ── Skilljar metric files ──────────────────────────────────────────────────

    def import_skilljar_metrics(self, folder_path: str):
        metric_files = glob.glob(os.path.join(folder_path, "**", "Skilljar Metric*"), recursive=True)
        if not metric_files: metric_files = glob.glob(os.path.join(folder_path, "Skilljar Metric*"))

        records = []
        for file in set(metric_files):
            df = load_dataframe(file)
            if df is None: continue
            cols = {str(c).lower().strip(): c for c in df.columns}
            email_col = cols.get("email")
            if not email_col: continue

            df = df.dropna(subset=[email_col])
            df = df[df[email_col].astype(str).str.contains("@", na=False)]

            for _, row in df.iterrows():
                email    = str(row.get(email_col, "")).strip()
                location = str(row.get(cols.get("location", "Location"), "N/A")).strip()
                title    = str(row.get(cols.get("title",    "Title"),    "N/A")).strip()
                manager  = str(row.get(cols.get("manager",  "Manager"),  "N/A")).strip()

                if location.lower() == "nan": location = "N/A"
                if title.lower()    == "nan": title    = "N/A"
                if manager.lower()  == "nan": manager  = "N/A"

                records.append((location, title, manager, parse_region(location), email))

        with get_connection() as conn:
            conn.executemany("UPDATE students SET location=?, title=?, manager=?, region_bucket=? WHERE LOWER(email)=LOWER(?)", records)

    # ── Course completion files ────────────────────────────────────────────────

    def import_course_files(self, folder_path: str):
        all_files = [
            os.path.join(root, f) for root, _, files in os.walk(folder_path) for f in files
            if f.lower().endswith((".csv", ".xlsx", ".xls"))
        ]

        course_records     = []
        completion_records = []

        for file in all_files:
            filename = os.path.basename(file).lower()
            if filename in ("students.csv", "students.xlsx") or filename.startswith("skilljar metric"): continue

            df = load_dataframe(file)
            if df is None: continue

            cols = {str(c).lower().strip(): c for c in df.columns}
            uid_col        = cols.get("user id")
            course_id_col  = cols.get("course id",   cols.get("course name",  cols.get("course title")))
            course_name_col = cols.get("course name", cols.get("course title", cols.get("course id")))
            pct_col        = cols.get("% complete",  cols.get("percent complete"))

            if not uid_col or not course_id_col: continue

            for _, row in df.iterrows():
                user_id    = row.get(uid_col)
                course_id  = row.get(course_id_col)
                course_name = row.get(course_name_col)

                if pd.isna(user_id) or pd.isna(course_id): continue

                uid_str        = normalize_uid(user_id)
                course_id_str  = str(course_id).strip()
                course_name_str = str(course_name).strip() if not pd.isna(course_name) else course_id_str
                clean_pct = safe_float(row.get(pct_col, 0) if pct_col else 0)

                course_records.append((course_id_str, course_name_str))
                completion_records.append((uid_str, course_id_str, clean_pct))

        with get_connection() as conn:
            # FIX: Prevent FOREIGN KEY constraint failure by silently adding missing UIDs
            unique_uids = list(set([r[0] for r in completion_records]))
            conn.executemany("INSERT OR IGNORE INTO students (user_id) VALUES (?)", [(u,) for u in unique_uids])

            conn.executemany(
                '''
                INSERT INTO courses (course_id, course_name)
                VALUES (?, ?)
                ON CONFLICT(course_id) DO UPDATE SET course_name = excluded.course_name
                ''', list(set(course_records))
            )
            conn.executemany(
                '''
                INSERT INTO completions (user_id, course_id, percent_complete)
                VALUES (?, ?, ?)
                ON CONFLICT(user_id, course_id) DO UPDATE SET percent_complete = excluded.percent_complete
                ''', completion_records
            )

            conn.execute('''
                UPDATE courses SET course_type = (
                    SELECT CASE 
                        WHEN SUM(CASE WHEN s.organization = 'FAE' THEN 1 ELSE 0 END) > 0 
                         AND SUM(CASE WHEN s.organization = 'Sales' THEN 1 ELSE 0 END) > 0 THEN 'Both'
                        WHEN SUM(CASE WHEN s.organization = 'FAE' THEN 1 ELSE 0 END) > 0 THEN 'FAE'
                        WHEN SUM(CASE WHEN s.organization = 'Sales' THEN 1 ELSE 0 END) > 0 THEN 'Sales'
                        ELSE 'Unknown'
                    END
                    FROM completions c
                    JOIN students s ON s.user_id = c.user_id
                    WHERE c.course_id = courses.course_id
                )
            ''')

    # ── Control codes ──────────────────────────────────────────────────────────

    def import_control_codes(self, folder_path: str):
        metric_files = glob.glob(os.path.join(folder_path, "**", "Skilljar Metric*"), recursive=True)
        if not metric_files: metric_files = glob.glob(os.path.join(folder_path, "Skilljar Metric*"))
        records = []

        for file in set(metric_files):
            df = load_dataframe(file, header_row=None)
            if df is None: continue

            try:
                first_row = [str(x).lower().strip() for x in df.iloc[0].values]
                if "control code" not in first_row and "email" not in first_row: continue

                try:
                    email_col_idx = first_row.index("email")
                except ValueError:
                    continue

                code_map = {}
                for search_row in range(1, min(6, len(df))):
                    row_vals = [str(df.iloc[search_row, c]).lower() for c in range(4)]
                    candidate = {}
                    for col_idx, val in enumerate(row_vals):
                        if "low" in val or "mid" in val: candidate[col_idx] = "Low & Mid Power"
                        elif "high" in val: candidate[col_idx] = "High Power"
                        elif "auto" in val: candidate[col_idx] = "Automotive"
                        elif "motor" in val: candidate[col_idx] = "Motor Driver"
                    if candidate:
                        code_map = candidate
                        data_start_row = search_row + 1
                        break

                if not code_map: continue

                for row_idx in range(data_start_row, len(df)):
                    email = str(df.iloc[row_idx, email_col_idx]).strip()
                    if not email or "@" not in email: continue

                    assigned_codes = [
                        code_name for col_idx, code_name in code_map.items()
                        if str(df.iloc[row_idx, col_idx]).strip() in ("1", "1.0", "1.00")
                    ]
                    if assigned_codes:
                        final = ", ".join(sorted(set(assigned_codes)))
                        records.append((final, email))

            except Exception as exc:
                print(f"[ERROR] parsing control codes from {file}: {exc}")

        with get_connection() as conn:
            conn.executemany("UPDATE students SET control_codes=? WHERE LOWER(email)=LOWER(?)", records)
</file>

<file path="database.py">
import sqlite3
import os
import sys
import json

if getattr(sys, 'frozen', False):
    BASE_DIR = os.path.dirname(sys.executable)
else:
    BASE_DIR = os.path.dirname(os.path.abspath(__file__))

DB_PATH = os.path.join(BASE_DIR, "powertrack.db")


def get_connection():
    conn = sqlite3.connect(DB_PATH)
    conn.execute("PRAGMA foreign_keys = ON")
    conn.execute("PRAGMA journal_mode = WAL")
    return conn


def setup_database():
    with get_connection() as conn:
        cursor = conn.cursor()

        # ── Core tables ────────────────────────────────────────────────────────

        cursor.execute('''
            CREATE TABLE IF NOT EXISTS students (
                user_id        TEXT PRIMARY KEY,
                first_name     TEXT,
                last_name      TEXT,
                email          TEXT,
                organization   TEXT DEFAULT 'Unknown',
                signed_up      TEXT,
                latest_activity TEXT,
                location       TEXT DEFAULT 'N/A',
                title          TEXT DEFAULT 'N/A',
                manager        TEXT DEFAULT 'N/A',
                progress_status TEXT DEFAULT 'N/A',
                control_codes  TEXT DEFAULT '',
                region_bucket  TEXT DEFAULT 'Worldwide'
            )
        ''')

        cursor.execute('''
            CREATE TABLE IF NOT EXISTS courses (
                course_id      TEXT PRIMARY KEY,
                course_name    TEXT,
                course_type    TEXT DEFAULT 'Unknown',
                control_code   TEXT DEFAULT 'Unassigned',
                graph_category TEXT DEFAULT 'Unassigned'
            )
        ''')

        cursor.execute('''
            CREATE TABLE IF NOT EXISTS completions (
                user_id          TEXT,
                course_id        TEXT,
                percent_complete REAL DEFAULT 0.0,
                completed_at     TEXT,
                score            TEXT,
                success_status   TEXT,
                PRIMARY KEY (user_id, course_id),
                FOREIGN KEY (user_id)   REFERENCES students (user_id),
                FOREIGN KEY (course_id) REFERENCES courses  (course_id)
            )
        ''')

        cursor.execute('''
            CREATE TABLE IF NOT EXISTS monthly_snapshots (
                month     TEXT,
                year      INTEGER DEFAULT 2025,
                team      TEXT,
                published INTEGER,
                taiwan    REAL,
                china     REAL,
                korea     REAL,
                japan     REAL,
                americas  REAL,
                europe    REAL,
                isea      REAL,
                worldwide REAL,
                PRIMARY KEY (month, year, team)
            )
        ''')

        # ── Settings table for dynamic configurations ──────────────────────────
        cursor.execute('''
            CREATE TABLE IF NOT EXISTS app_settings (
                key TEXT PRIMARY KEY,
                value TEXT
            )
        ''')

        cursor.execute("SELECT 1 FROM app_settings WHERE key='category_tree'")
        if not cursor.fetchone():
            default_tree = {
                "Low & Mid Power": ["InnoSwitch", "LinkSwitch", "LYTSwitch", "Other"],
                "High Power":      ["High Power"],
                "Automotive":      ["Automotive"],
                "Motor Driver":    ["Motor Driver"]
            }
            cursor.execute("INSERT INTO app_settings (key, value) VALUES ('category_tree', ?)", (json.dumps(default_tree),))


        # ── Schema-migration table (versioned) ─────────────────────────────────
        cursor.execute('''
            CREATE TABLE IF NOT EXISTS schema_migrations (
                id         INTEGER PRIMARY KEY,
                applied_at TEXT DEFAULT (datetime('now'))
            )
        ''')

        def applied(mid):
            return cursor.execute(
                "SELECT 1 FROM schema_migrations WHERE id = ?", (mid,)
            ).fetchone() is not None

        def run(mid, sql):
            if not applied(mid):
                try:
                    cursor.execute(sql)
                    cursor.execute(
                        "INSERT INTO schema_migrations (id) VALUES (?)", (mid,)
                    )
                except sqlite3.OperationalError as exc:
                    if "duplicate column" not in str(exc).lower():
                        raise

        run(1, "ALTER TABLE students ADD COLUMN control_codes TEXT DEFAULT ''")
        run(2, "ALTER TABLE students ADD COLUMN region_bucket TEXT DEFAULT 'Worldwide'")
        run(3, "ALTER TABLE courses  ADD COLUMN control_code  TEXT DEFAULT 'Unassigned'")
        run(4, "ALTER TABLE courses  ADD COLUMN graph_category TEXT DEFAULT 'Unassigned'")
        run(5, "ALTER TABLE monthly_snapshots ADD COLUMN year INTEGER DEFAULT 2025")
        
        run(6, '''
            CREATE TABLE IF NOT EXISTS completions_history (
                user_id          TEXT,
                course_id        TEXT,
                year             INTEGER,
                month            INTEGER,
                percent_complete REAL,
                PRIMARY KEY (user_id, course_id, year, month)
            )
        ''')
        
        # Org-Isolated Course Mappings and Sort Orders
        run(7, "ALTER TABLE courses ADD COLUMN fae_control_code TEXT DEFAULT 'Unassigned'")
        run(8, "ALTER TABLE courses ADD COLUMN fae_graph_category TEXT DEFAULT 'Unassigned'")
        run(9, "ALTER TABLE courses ADD COLUMN fae_sort_order INTEGER DEFAULT 999")
        
        run(10, "ALTER TABLE courses ADD COLUMN sales_control_code TEXT DEFAULT 'Unassigned'")
        run(11, "ALTER TABLE courses ADD COLUMN sales_graph_category TEXT DEFAULT 'Unassigned'")
        run(12, "ALTER TABLE courses ADD COLUMN sales_sort_order INTEGER DEFAULT 999")
        
        run(13, "ALTER TABLE courses ADD COLUMN unknown_control_code TEXT DEFAULT 'Unassigned'")
        run(14, "ALTER TABLE courses ADD COLUMN unknown_graph_category TEXT DEFAULT 'Unassigned'")
        run(15, "ALTER TABLE courses ADD COLUMN unknown_sort_order INTEGER DEFAULT 999")
        
        # Backfill existing data safely
        run(16, """
            UPDATE courses SET 
                fae_control_code = control_code, fae_graph_category = graph_category,
                sales_control_code = control_code, sales_graph_category = graph_category,
                unknown_control_code = control_code, unknown_graph_category = graph_category
            WHERE fae_control_code = 'Unassigned' AND control_code != 'Unassigned'
        """)

        # ── Indexes ────────────────────────────────────────────────────────────
        cursor.execute("CREATE INDEX IF NOT EXISTS idx_completions_user   ON completions (user_id)")
        cursor.execute("CREATE INDEX IF NOT EXISTS idx_completions_course ON completions (course_id)")
        cursor.execute("CREATE INDEX IF NOT EXISTS idx_students_email ON students (email)")
        cursor.execute("CREATE INDEX IF NOT EXISTS idx_students_org   ON students (organization)")


setup_database()
</file>

<file path="design.ui">
<?xml version="1.0" encoding="UTF-8"?>
<ui version="4.0">
 <class>MainWindow</class>
 <widget class="QMainWindow" name="MainWindow">
  <property name="geometry">
   <rect><x>0</x><y>0</y><width>1200</width><height>800</height></rect>
  </property>
  <property name="windowTitle">
   <string>Power Track</string>
  </property>
  <property name="styleSheet">
   <string notr="true">
    QMainWindow { background: transparent; }
    QWidget#centralwidget { background: #16161a; border: 1px solid #3a3a40; border-radius: 12px; }
    QFrame#headerr, QFrame#footer { background: transparent; border: none; }
    QFrame#selectionframe { background: #202025; border: 1px solid #3a3a40; border-radius: 8px; }
    
    /* Buttons */
    #centralwidget QPushButton { padding: 0px 15px; background: #0085ca; color: white; border-radius: 5px; font-weight: bold; min-height: 28px; }
    #centralwidget QPushButton:hover { background: #3c649f; }
    #centralwidget QPushButton:disabled { background: #333333; color: #777777; }
    #headerr QPushButton { padding: 0px; }
    
    /* Main Tabs Navigation */
    QPushButton#btn_tab_students, QPushButton#btn_tab_matrix, QPushButton#btn_tab_analytics {
        padding: 0px 10px; background-color: transparent; color: #777777; border: none; border-bottom: 3px solid transparent; border-radius: 0px; font-size: 14px;
    }
    QPushButton#btn_tab_students:hover, QPushButton#btn_tab_matrix:hover, QPushButton#btn_tab_analytics:hover { background-color: #2a2a30; }
    QPushButton#btn_tab_students:checked, QPushButton#btn_tab_matrix:checked, QPushButton#btn_tab_analytics:checked { color: white; border-bottom: 3px solid #0085ca; font-weight: bold; }

    /* Inputs */
    #centralwidget QLineEdit, #centralwidget QComboBox { background: #1a1a1e; color: #e0e0e0; border-radius: 4px; padding: 4px 5px; border: 1px solid #3a3a40; min-height: 20px;}
    
    /* Group Boxes */
    QGroupBox { color: #f0f0f0; border: 1px solid #3a3a40; border-radius: 5px; margin-top: 10px; font-weight: bold; }
    QGroupBox::title { subcontrol-origin: margin; subcontrol-position: top left; padding: 0 5px; }
    
    /* Global TabWidget / TabBar */
    QTabWidget::pane { border: none; background: transparent; }
    QTabBar { qproperty-expanding: 1; }
    QTabWidget::tab-bar { left: 0px; right: 0px; alignment: center; }
    QTabBar::tab { 
        background: #202025; 
        color: #777777; 
        border: 1px solid #3a3a40; 
        padding: 8px 15px; 
        font-size: 14px; 
        min-width: 120px;
        min-height: 20px;
    }
    QTabBar::tab:selected { background: #0085ca; color: white; border: 1px solid #0085ca; font-weight: bold; }
    QTabBar::tab:hover:!selected { background: #2a2a30; }
    
    /* Tables */
    QTableWidget, QTableView { alternate-background-color: #1a1a1e; background-color: #121214; color: #f0f0f0; border: 1px solid #3a3a40; border-radius: 5px; font-size: 13px; gridline-color: #3a3a40; }
    QTableWidget::viewport, QTableView::viewport { background-color: #121214; }
    QHeaderView { background-color: #121214; border: none; }
    QHeaderView::section { background-color: #202025; color: white; padding: 6px; border: 1px solid #3a3a40; font-weight: bold; font-size: 13px; }
    QTableWidget::item:selected, QTableView::item:selected { background-color: #0085ca; color: white; }
    QTableCornerButton::section { background-color: #202025; border: 1px solid #3a3a40; }
    
    /* Matrix Headers specifically */
    QTableWidget#header_table { border-bottom: none; border-bottom-left-radius: 0px; border-bottom-right-radius: 0px; }
    QTableWidget#table_matrix { border-top-left-radius: 0px; border-top-right-radius: 0px; }
    
    /* Scroll Areas */
    QScrollArea { border: 1px solid #3a3a40; background-color: #121214; border-radius: 4px; }

    #centralwidget QLabel { color: white; border: none; }
    
    /* Color Specific Buttons */
    #btn_import_monthly { background: #ffaa00; color: #121214; }
    #btn_import_monthly:hover { background: #cc8800; }
    #btn_refresh_matrix { background: #87ceeb; color: #121214; }
    #btn_refresh_matrix:hover { background: #5bb2d4; }
    #btn_clear_filters, #btn_clear_matrix { background: #f08080; color: #121214; }
    #btn_clear_filters:hover, #btn_clear_matrix:hover { background: #cd5c5c; }
    #btn_load_trend { background: #00B050; color: white; }
    #btn_load_trend:hover { background: #008a3e; }
    #btn_export_archive { background: #00B050; color: white; }
    #btn_export_archive:hover { background: #008a3e; }
   </string>
  </property>
  <widget class="QWidget" name="centralwidget">
   <layout class="QVBoxLayout" name="verticalLayout_main">
    <property name="spacing"><number>0</number></property>
    <property name="margin"><number>0</number></property>
    <item>
     <widget class="QFrame" name="headerr">
      <property name="minimumSize"><size><width>0</width><height>50</height></size></property>
      <layout class="QHBoxLayout" name="horizontalLayout_header">
       <item><widget class="QLabel" name="data_exporter"><property name="font"><font><family>Bahnschrift</family><pointsize>16</pointsize><bold>true</bold></font></property><property name="text"><string>Power Track</string></property></widget></item>
       <item><spacer><property name="orientation"><enum>Qt::Horizontal</enum></property></spacer></item>
       <item><widget class="QPushButton" name="minimize_button"><property name="maximumSize"><size><width>30</width><height>30</height></size></property><property name="text"><string>—</string></property></widget></item>
       <item><widget class="QPushButton" name="maximize_button"><property name="maximumSize"><size><width>30</width><height>30</height></size></property><property name="text"><string>▢</string></property></widget></item>
       <item><widget class="QPushButton" name="exit_button"><property name="maximumSize"><size><width>30</width><height>30</height></size></property><property name="text"><string>✕</string></property></widget></item>
      </layout>
     </widget>
    </item>
    <item>
     <layout class="QHBoxLayout" name="horizontalLayout_body">
      <property name="margin"><number>15</number></property>
      <property name="spacing"><number>15</number></property>
      
      <!-- MAIN PANEL -->
      <item>
       <widget class="QFrame" name="selectionframe">
        <layout class="QVBoxLayout" name="verticalLayout_selection">
         <property name="margin"><number>15</number></property>
         <property name="spacing"><number>15</number></property>
         
         <!-- NAVIGATION -->
         <item>
          <layout class="QHBoxLayout" name="tab_layout">
           <property name="spacing"><number>0</number></property>
           <item>
            <widget class="QPushButton" name="btn_tab_students">
             <property name="minimumSize"><size><width>120</width><height>35</height></size></property>
             <property name="text"><string>Students</string></property>
             <property name="checkable"><bool>true</bool></property>
             <property name="checked"><bool>true</bool></property>
            </widget>
           </item>
           <item>
            <widget class="QPushButton" name="btn_tab_matrix">
             <property name="minimumSize"><size><width>120</width><height>35</height></size></property>
             <property name="text"><string>Course Matrix</string></property>
             <property name="checkable"><bool>true</bool></property>
            </widget>
           </item>
           <item>
            <widget class="QPushButton" name="btn_tab_analytics">
             <property name="minimumSize"><size><width>120</width><height>35</height></size></property>
             <property name="text"><string>Analytics</string></property>
             <property name="checkable"><bool>true</bool></property>
            </widget>
           </item>
           <item><spacer><property name="orientation"><enum>Qt::Horizontal</enum></property></spacer></item>
          </layout>
         </item>
         
         <item>
          <widget class="QStackedWidget" name="stackedWidget">
           <property name="currentIndex"><number>0</number></property>
           
           <!-- PAGE 0: STUDENTS -->
           <widget class="QWidget" name="page_students">
            <layout class="QVBoxLayout" name="layout_page_students">
             <property name="margin"><number>0</number></property>
             <item>
              <widget class="QGroupBox" name="group_filters">
               <property name="title"><string>Filters</string></property>
               <layout class="QHBoxLayout">
                <item><widget class="QLabel"><property name="text"><string>Search:</string></property></widget></item>
                <item><widget class="QLineEdit" name="search_student"></widget></item>
                <item><widget class="QLabel"><property name="text"><string>Location/Region:</string></property></widget></item>
                <item><widget class="QComboBox" name="combo_region"></widget></item>
                <item><widget class="QLabel"><property name="text"><string>Progress:</string></property></widget></item>
                <item><widget class="QComboBox" name="combo_progress"></widget></item>
                <item><widget class="QPushButton" name="btn_clear_filters"><property name="text"><string>Clear Filters</string></property></widget></item>
                <item><widget class="QPushButton" name="btn_edit_progress"><property name="text"><string>Edit Logic</string></property><property name="styleSheet"><string>background: #0085ca; color: white;</string></property></widget></item>
                 <item><widget class="QPushButton" name="btn_email_students"><property name="text"><string>Email Students</string></property><property name="styleSheet"><string>background: #0085ca; color: white;</string></property></widget></item>
                <item><widget class="QLabel" name="lbl_showing"><property name="text"><string>Showing all 0 students</string></property><property name="styleSheet"><string>color: #5bb2d4;</string></property></widget></item>
                <item><spacer><property name="orientation"><enum>Qt::Horizontal</enum></property></spacer></item>
               </layout>
              </widget>
             </item>
             
             <!-- FIXED SUB TABS (No expanding container) -->
             <item>
              <widget class="QTabBar" name="tabBar_students">
              </widget>
             </item>

             <item>
              <layout class="QHBoxLayout">
               <item><widget class="QLabel" name="lbl_total"><property name="text"><string>&lt;b&gt;Total: 0&lt;/b&gt;</string></property></widget></item>
               <item><widget class="QLabel" name="lbl_internal"><property name="text"><string>Internal: 0</string></property></widget></item>
               <item><widget class="QLabel" name="lbl_external"><property name="text"><string>External: 0</string></property></widget></item>
               <item><spacer><property name="orientation"><enum>Qt::Horizontal</enum></property></spacer></item>
              </layout>
             </item>
             <item>
              <widget class="QTableView" name="table_students">
              </widget>
             </item>
             <item>
              <layout class="QHBoxLayout">
               <item><widget class="QLabel" name="lbl_current_month"><property name="text"><string>Current Data View: None</string></property><property name="styleSheet"><string>color: #87ceeb; font-weight: bold; font-size: 18px; padding-left: 20px;</string></property></widget></item>
               <item><spacer><property name="orientation"><enum>Qt::Horizontal</enum></property></spacer></item>
               <item><widget class="QPushButton" name="btn_import_monthly"><property name="text"><string>Import Monthly Data</string></property></widget></item>
               <item><widget class="QPushButton" name="btn_export_archive"><property name="text"><string>Export Month Archive</string></property></widget></item>
              </layout>
             </item>
            </layout>
           </widget>
           
           <!-- PAGE 1: COURSE MATRIX -->
           <widget class="QWidget" name="page_matrix">
            <layout class="QVBoxLayout" name="layout_page_matrix">
             <property name="margin"><number>0</number></property>
             <item>
              <widget class="QGroupBox" name="group_matrix_filters">
               <property name="title"><string>Matrix Filters</string></property>
               <layout class="QHBoxLayout">
                <item><widget class="QLabel"><property name="text"><string>Search:</string></property></widget></item>
                <item><widget class="QLineEdit" name="search_matrix"></widget></item>
                <item><widget class="QLabel"><property name="text"><string>Control Code:</string></property></widget></item>
                <item><widget class="QComboBox" name="combo_matrix_code"></widget></item>
                <item><widget class="QLabel"><property name="text"><string>Region:</string></property></widget></item>
                <item><widget class="QComboBox" name="combo_matrix_region"></widget></item>
                <item><spacer><property name="orientation"><enum>Qt::Horizontal</enum></property></spacer></item>
               </layout>
              </widget>
             </item>
             
             <!-- FIXED SUB TABS (No expanding container) -->
             <item>
              <widget class="QTabBar" name="tabBar_matrix">
              </widget>
             </item>

             <!-- BUNDLED HEADER AND TABLE -->
             <item>
              <layout class="QVBoxLayout" name="layout_matrix_tables">
               <property name="spacing"><number>0</number></property>
               <item>
                <widget class="QTableWidget" name="header_table">
                 <property name="horizontalScrollBarPolicy"><enum>Qt::ScrollBarAlwaysOff</enum></property>
                 <property name="focusPolicy"><enum>Qt::NoFocus</enum></property>
                 <property name="wordWrap"><bool>true</bool></property>
                 <attribute name="horizontalHeaderVisible">
                  <bool>false</bool>
                 </attribute>
                </widget>
               </item>
               <item>
                <widget class="QTableView" name="table_matrix">
                 <attribute name="horizontalHeaderVisible">
                  <bool>false</bool>
                 </attribute>
                </widget>
               </item>
              </layout>
             </item>
             
             <item>
              <layout class="QHBoxLayout">
               <item><widget class="QPushButton" name="btn_refresh_matrix"><property name="text"><string>Map Courses</string></property></widget></item>
               <item><widget class="QPushButton" name="btn_clear_matrix"><property name="text"><string>Export Matrix</string></property></widget></item>
               <item><spacer><property name="orientation"><enum>Qt::Horizontal</enum></property></spacer></item>
              </layout>
             </item>
            </layout>
           </widget>

           <!-- PAGE 2: ANALYTICS -->
           <widget class="QWidget" name="page_analytics">
            <layout class="QVBoxLayout" name="layout_page_analytics">
             <property name="margin"><number>0</number></property>
             <item>
              <widget class="QTabWidget" name="tabWidget_analytics">
               <property name="documentMode">
                <bool>true</bool>
               </property>
               
               <!-- FAE ANALYTICS TAB -->
               <widget class="QWidget" name="tab_ana_fae">
                <attribute name="title"><string>FAE</string></attribute>
                <layout class="QVBoxLayout">
                 <property name="margin"><number>0</number></property>
                 <item>
                  <widget class="QTabWidget" name="tab_fae">
                   <property name="documentMode">
                    <bool>true</bool>
                   </property>
                   <widget class="QWidget" name="tab_fae_table">
                    <attribute name="title"><string>Table</string></attribute>
                    <layout class="QVBoxLayout"><property name="margin"><number>0</number></property>
                     <item><widget class="QTableWidget" name="table_fae"></widget></item>
                    </layout>
                   </widget>
                   <widget class="QWidget" name="tab_fae_charts">
                    <attribute name="title"><string>Charts</string></attribute>
                    <layout class="QVBoxLayout"><property name="margin"><number>0</number></property>
                     <item><widget class="QScrollArea" name="scroll_fae"><property name="widgetResizable"><bool>true</bool></property></widget></item>
                    </layout>
                   </widget>
                  </widget>
                 </item>
                 <item>
                  <layout class="QHBoxLayout">
                   <item><spacer><property name="orientation"><enum>Qt::Horizontal</enum></property></spacer></item>
                   <item><widget class="QPushButton" name="btn_export_fae"><property name="text"><string>Export FAE</string></property><property name="minimumHeight"><number>35</number></property></widget></item>
                  </layout>
                 </item>
                </layout>
               </widget>

               <!-- SALES ANALYTICS TAB -->
               <widget class="QWidget" name="tab_ana_sales">
                <attribute name="title"><string>Sales</string></attribute>
                <layout class="QVBoxLayout">
                 <property name="margin"><number>0</number></property>
                 <item>
                  <widget class="QTabWidget" name="tab_sales">
                   <property name="documentMode">
                    <bool>true</bool>
                   </property>
                   <widget class="QWidget" name="tab_sales_table">
                    <attribute name="title"><string>Table</string></attribute>
                    <layout class="QVBoxLayout"><property name="margin"><number>0</number></property>
                     <item><widget class="QTableWidget" name="table_sales"></widget></item>
                    </layout>
                   </widget>
                   <widget class="QWidget" name="tab_sales_charts">
                    <attribute name="title"><string>Charts</string></attribute>
                    <layout class="QVBoxLayout"><property name="margin"><number>0</number></property>
                     <item><widget class="QScrollArea" name="scroll_sales"><property name="widgetResizable"><bool>true</bool></property></widget></item>
                    </layout>
                   </widget>
                  </widget>
                 </item>
                 <item>
                  <layout class="QHBoxLayout">
                   <item><spacer><property name="orientation"><enum>Qt::Horizontal</enum></property></spacer></item>
                   <item><widget class="QPushButton" name="btn_export_sales"><property name="text"><string>Export Sales</string></property><property name="minimumHeight"><number>35</number></property></widget></item>
                  </layout>
                 </item>
                </layout>
               </widget>

               <!-- SUMMARY ANALYTICS TAB -->
               <widget class="QWidget" name="tab_ana_summary">
                <attribute name="title"><string>Summary</string></attribute>
                <layout class="QVBoxLayout">
                 <property name="margin"><number>0</number></property>
                 <item>
                  <widget class="QTabWidget" name="tab_summary">
                   <property name="documentMode">
                    <bool>true</bool>
                   </property>
                   
                   <widget class="QWidget" name="tab_summary_current_table">
                    <attribute name="title"><string>Current Table</string></attribute>
                    <layout class="QVBoxLayout"><property name="margin"><number>0</number></property>
                     <item><widget class="QTableWidget" name="table_summary_current"></widget></item>
                    </layout>
                   </widget>

                   <widget class="QWidget" name="tab_summary_current_charts">
                    <attribute name="title"><string>Current Charts</string></attribute>
                    <layout class="QVBoxLayout"><property name="margin"><number>0</number></property>
                     <item><widget class="QScrollArea" name="scroll_summary_current"><property name="widgetResizable"><bool>true</bool></property></widget></item>
                    </layout>
                   </widget>

                   <widget class="QWidget" name="tab_summary_trend_table">
                    <attribute name="title"><string>Trend Table</string></attribute>
                    <layout class="QVBoxLayout"><property name="margin"><number>0</number></property>
                     <item><widget class="QTableWidget" name="table_summary_trend"></widget></item>
                    </layout>
                   </widget>

                   <widget class="QWidget" name="tab_summary_trend_charts">
                    <attribute name="title"><string>Trend Charts</string></attribute>
                    <layout class="QVBoxLayout"><property name="margin"><number>0</number></property>
                     <item><widget class="QScrollArea" name="scroll_summary_trend"><property name="widgetResizable"><bool>true</bool></property></widget></item>
                    </layout>
                   </widget>

                  </widget>
                 </item>
                 <item>
                  <layout class="QHBoxLayout">
                   <item><spacer><property name="orientation"><enum>Qt::Horizontal</enum></property></spacer></item>
                   <item><widget class="QPushButton" name="btn_load_trend">
                     <property name="text"><string>Load Summary Folder</string></property>
                     <property name="minimumHeight"><number>35</number></property>
                   </widget></item>
                   <item><widget class="QPushButton" name="btn_export_current_summary">
                     <property name="text"><string>Export Current Month</string></property>
                     <property name="minimumHeight"><number>35</number></property>
                   </widget></item>
                  </layout>
                 </item>
                </layout>
               </widget>

              </widget>
             </item>
            </layout>
           </widget>
           
          </widget>
         </item>
        </layout>
       </widget>
      </item>
     </layout>
    </item>
   </layout>
  </widget>
 </widget>
 <resources/>
 <connections/>
</ui>
</file>

<file path="main.py">
import sys
from PyQt5.QtWidgets import QApplication, QMainWindow
from PyQt5 import uic
from PyQt5.QtCore import Qt, QPoint
from PyQt5.QtGui import QFont
import os
from components.tab_students import StudentTabComponent
from components.tab_matrix import MatrixTabComponent 
from components.tab_analytics import AnalyticsTabComponent

def resource_path(relative_path):
    """ Get absolute path to resource, works for dev and for PyInstaller """
    try:
        base_path = sys._MEIPASS
    except Exception:
        base_path = os.path.abspath(".")
    return os.path.join(base_path, relative_path)

class MainWindow(QMainWindow):
    def __init__(self):
        super().__init__()
        uic.loadUi(resource_path('design.ui'), self)

        # OVERRIDE CSS FONTS TO MAKE THEM BIGGER (Scaling up by ~35-40%)
        current_style = self.styleSheet()
        # Scale up the specific hard-coded sizes in the stylesheet
        new_style = current_style.replace('font-size: 13px;', 'font-size: 18px;')
        new_style = new_style.replace('font-size: 14px;', 'font-size: 19px;')
        self.setStyleSheet(new_style)

        self.setWindowFlags(Qt.FramelessWindowHint)
        self.setAttribute(Qt.WA_TranslucentBackground)

        self.tab_buttons = [
            self.btn_tab_students,
            self.btn_tab_matrix,
            self.btn_tab_analytics
        ]

        self.btn_tab_students.clicked.connect(lambda: self.switch_tab(0))
        self.btn_tab_matrix.clicked.connect(lambda: self.switch_tab(1))
        self.btn_tab_analytics.clicked.connect(lambda: self.switch_tab(2))

        self.exit_button.clicked.connect(self.close)
        self.minimize_button.clicked.connect(self.showMinimized)
        if hasattr(self, 'maximize_button'):
            self.maximize_button.clicked.connect(self.toggle_maximize)

        self.oldPos = self.pos()

        # INITIALIZE THE TAB COMPONENTS
        self.student_tab = StudentTabComponent(self) 
        self.matrix_tab = MatrixTabComponent(self) 
        self.analytics_tab = AnalyticsTabComponent(self)
        
        self.switch_tab(0)

    def switch_tab(self, index):
        self.stackedWidget.setCurrentIndex(index)
        
        for i, btn in enumerate(self.tab_buttons):
            btn.setChecked(i == index)
            
        if index == 1: self.matrix_tab.load_matrix_data()
        if index == 2: self.analytics_tab.update_data()

    def toggle_maximize(self):
        if self.windowState() == Qt.WindowMaximized:
            self.setWindowState(Qt.WindowNoState)
        else:
            # Get the screen the app is currently on
            screen = QApplication.desktop().screenNumber(self)
            # Find the available space (which explicitly subtracts the Windows Taskbar)
            available_geom = QApplication.desktop().availableGeometry(screen)
            
            # Restrict the app from growing larger than the space minus the taskbar
            self.setMaximumSize(available_geom.size())
            self.setWindowState(Qt.WindowMaximized)

    def mousePressEvent(self, event):
        if event.button() == Qt.LeftButton:
            self.oldPos = event.globalPos()

    def mouseMoveEvent(self, event):
        if event.buttons() == Qt.LeftButton:
            delta = QPoint(event.globalPos() - self.oldPos)
            self.move(self.x() + delta.x(), self.y() + delta.y())
            self.oldPos = event.globalPos()

if __name__ == "__main__":
    app = QApplication(sys.argv)
    
    # APPLY GLOBAL FONT SCALING ACROSS ALL LABELS, BUTTONS, DROPDOWNS
    font = app.font()
    current_size = font.pointSize()
    if current_size <= 0:  # If Qt fails to detect it, assume standard 9pt
        current_size = 9
    
    # Increase base font size by ~35%
    font.setPointSize(int(current_size * 1.35))
    app.setFont(font)
    
    window = MainWindow()
    window.show()
    sys.exit(app.exec_())
</file>

<file path="components/tab_analytics.py">
import csv
import io
import os
import glob
import re

from PyQt5.QtWidgets import (
    QTableWidgetItem, QHeaderView, QFileDialog, QMessageBox, QApplication,
)
from PyQt5.QtCore import Qt, QSettings
import pandas as pd
import matplotlib.pyplot as plt
import matplotlib.patches as mpatches
from matplotlib.backends.backend_qt5agg import FigureCanvasQTAgg as FigureCanvas
import numpy as np
import sqlite3

from data_manager import safe_float, normalize_uid
from database import DB_PATH

# ── Team Analytics Controller (FAE & Sales) ───────────────────────────────────

class TeamAnalyticsController:
    """Controller linking UI elements from design.ui to analytics logic for FAE and Sales."""

    def __init__(self, team_name, table, scroll_area, btn_export, main_ui):
        self.team_name  = team_name
        self.table      = table
        self.scroll_area = scroll_area
        self.btn_export = btn_export
        self.main_ui    = main_ui

        self.analytics_data = {}
        self.regions        = []
        self.categories     = []
        
        self.is_loaded      = False

        self.setup_ui_elements()
        self.btn_export.clicked.connect(self.export_analytics)

    def setup_ui_elements(self):
        self.table.verticalHeader().setVisible(False)
        self.table.verticalHeader().setDefaultSectionSize(40)

        if hasattr(self, "figure"):
            plt.close(self.figure)

        self.figure = plt.figure(figsize=(11, 12))
        self.figure.patch.set_facecolor("#121214")
        self.canvas = FigureCanvas(self.figure)
        self.canvas.setStyleSheet("background-color: transparent;")
        self.canvas.setMinimumSize(1800, 1800)
        
        self.scroll_area.setWidget(self.canvas)

    def auto_load_data(self):
        if self.is_loaded:
            return

        settings = QSettings("PowerIntegrations", "PowerTrack")
        folder   = settings.value(
            "current_data_folder",
            os.path.join(os.path.dirname(os.path.abspath(__file__)), "..", "data"),
        )

        pattern1 = os.path.join(folder, f"*Skilljar Metric*{self.team_name}*.csv")
        pattern2 = os.path.join(folder, "**", f"*Skilljar Metric*{self.team_name}*.csv")
        files = list(set(glob.glob(pattern1) + glob.glob(pattern2, recursive=True)))

        loaded_from_csv = False

        if files:
            if len(files) > 1:
                files.sort(key=os.path.getmtime, reverse=True)
            try:
                self.parse_csv_for_analytics(files[0])
                loaded_from_csv = True
            except Exception as exc:
                print(f"[WARN] Failed to parse CSV for {self.team_name}: {exc}. Falling back to DB.")

        if not loaded_from_csv:
            try:
                self.calculate_analytics_from_db()
                print(f"[INFO] Successfully loaded {self.team_name} analytics from Database fallback.")
            except Exception as exc:
                print(f"[ERROR] DB Fallback failed for {self.team_name}: {exc}")
                self.btn_export.setEnabled(False)
                self.table.clearContents()
                plt.close(self.figure)
                self.figure = plt.figure(figsize=(11, 12))
                self.figure.patch.set_facecolor("#121214")
                self.canvas.figure = self.figure
                self.canvas.draw()
                self.is_loaded = True 
                return

        self.populate_table()
        self.draw_charts()
        self.btn_export.setEnabled(True)
        self.is_loaded = True

    def calculate_analytics_from_db(self):
        self.analytics_data.clear()
        self.regions.clear()
        self.categories.clear()

        self.regions = ["Taiwan", "China", "Korea", "Japan", "US/Canada", "Europe", "ISEA", "Worldwide"]
        valid_cats = [
            "InnoSwitch", "LinkSwitch", "LYTSwitch", "Other",
            "Motor Driver", "Automotive", "High Power", "All Course"
        ]
        self.categories = valid_cats

        db_region_map = {
            "Taiwan": "Taiwan",
            "China": "China",
            "Korea": "Korea",
            "Japan": "Japan",
            "Americas": "US/Canada",
            "Europe": "Europe",
            "ISEA": "ISEA"
        }

        with sqlite3.connect(DB_PATH) as conn:
            cursor = conn.cursor()
            prefix = self.team_name.lower()
            cursor.execute(f"SELECT course_id, {prefix}_control_code, {prefix}_graph_category FROM courses")
            course_map = {str(r[0]): (r[1], r[2]) for r in cursor.fetchall()}

            cursor.execute(
                "SELECT user_id, region_bucket, control_codes FROM students WHERE organization = ?",
                (self.team_name,)
            )
            students = cursor.fetchall()

            cursor.execute(
                """
                SELECT c.user_id, c.course_id, c.percent_complete
                FROM completions c
                JOIN students s ON s.user_id = c.user_id
                WHERE s.organization = ?
                """,
                (self.team_name,)
            )
            completions_map = {}
            for uid, cid, pct in cursor.fetchall():
                uid_str = normalize_uid(uid)
                completions_map.setdefault(uid_str, {})[str(cid)] = safe_float(pct)

        cat_totals = {cat: {reg: [] for reg in self.regions} for cat in valid_cats}

        for st in students:
            user_id, region_bucket, control_codes = st
            uid_str = normalize_uid(user_id)

            my_codes = (
                [c.strip() for c in str(control_codes).split(",")]
                if control_codes and str(control_codes).lower() not in ("nan", "n/a", "none", "")
                else []
            )
            if not my_codes:
                continue

            my_req_courses = [cid for cid, (cc, gc) in course_map.items() if cc in my_codes]
            if not my_req_courses:
                continue

            courses_by_cat = {cat: [] for cat in valid_cats}
            for cid in my_req_courses:
                gc = course_map[cid][1]
                if gc in valid_cats:
                    courses_by_cat[gc].append(cid)
                courses_by_cat["All Course"].append(cid)

            chart_region = db_region_map.get(region_bucket)
            student_completions = completions_map.get(uid_str, {})

            for cat, cids in courses_by_cat.items():
                if not cids:
                    continue
                total_pct = sum(student_completions.get(cid, 0.0) for cid in cids)
                avg_pct = total_pct / len(cids)
                cat_totals[cat]["Worldwide"].append(avg_pct)
                if chart_region:
                    cat_totals[cat][chart_region].append(avg_pct)

        for cat in valid_cats:
            row_data = []
            for reg in self.regions:
                vals = cat_totals[cat][reg]
                row_data.append((sum(vals) / len(vals)) if vals else 0.0)
            self.analytics_data[cat] = row_data

    def parse_csv_for_analytics(self, filepath):
        self.analytics_data.clear()
        self.regions.clear()
        self.categories.clear()

        with open(filepath, "r", encoding="utf-8", errors="ignore") as f:
            rows = list(csv.reader(f))

        start_r, start_c = next(
            ((r, c) for r, row in enumerate(rows) for c, cell in enumerate(row) if cell.strip() == "Category Team"),
            (-1, -1),
        )
        if start_r == -1:
            raise ValueError("Could not find 'Category Team' summary block.")

        self.regions = [x.strip() for x in rows[start_r][start_c + 1: start_c + 9]]
        valid_cats = [
            "InnoSwitch", "LinkSwitch", "LYTSwitch", "Other",
            "Motor Driver", "Automotive", "High Power", "All Course",
        ]

        for r_idx in range(start_r + 1, min(start_r + 20, len(rows))):
            cat = rows[r_idx][start_c].strip()
            if cat in valid_cats:
                self.categories.append(cat)
                self.analytics_data[cat] = [
                    safe_float(v) for v in rows[r_idx][start_c + 1: start_c + 9]
                ]

    def populate_table(self):
        self.table.setUpdatesEnabled(False)
        self.table.clear()

        headers = ["Category"] + self.regions
        self.table.setColumnCount(len(headers))
        self.table.setHorizontalHeaderLabels(headers)
        self.table.setRowCount(len(self.categories))

        for r_idx, cat in enumerate(self.categories):
            item_cat = QTableWidgetItem(cat)
            item_cat.setTextAlignment(Qt.AlignCenter | Qt.AlignVCenter)
            
            font = item_cat.font()
            font.setBold(True)
            item_cat.setFont(font)
            
            self.table.setItem(r_idx, 0, item_cat)

            for c_idx, val in enumerate(self.analytics_data[cat]):
                item_val = QTableWidgetItem(
                    f"{val:.2f}%" if val > 0 else "0.00%"
                )
                item_val.setTextAlignment(Qt.AlignCenter | Qt.AlignVCenter)
                if val == 0:
                    item_val.setForeground(Qt.gray)
                self.table.setItem(r_idx, c_idx + 1, item_val)

        self.table.horizontalHeader().setMinimumSectionSize(110)
        if self.table.columnCount() > 0:
            self.table.horizontalHeader().setSectionResizeMode(0, QHeaderView.Stretch)
            for i in range(1, self.table.columnCount()):
                self.table.horizontalHeader().setSectionResizeMode(i, QHeaderView.Interactive)
                self.table.setColumnWidth(i, 110)

        self.table.setUpdatesEnabled(True)

    def draw_charts(self):
        plt.close(self.figure)
        self.figure  = plt.figure(figsize=(11, 12))
        self.figure.patch.set_facecolor("#121214")
        self.canvas.figure = self.figure

        if not self.categories:
            self.canvas.draw()
            return

        chart_cats   = [c for c in self.categories if c != "All Course"]
        regions_no_ww = self.regions[:-1]
        ww_idx       = self.regions.index("Worldwide")

        ax1 = self.figure.add_subplot(211)
        ax1.set_facecolor("#121214")
        ax1.grid(axis="y", linestyle="-", color="#3a3a40", alpha=0.7, zorder=0)
        x, width = np.arange(len(chart_cats)), 0.11
        reg_colors = ["#4472C4", "#ED7D31", "#A5A5A5", "#FFC000", "#5B9BD5", "#00B050", "#264478"]

        for i, reg in enumerate(regions_no_ww):
            ax1.bar(
                x + (width * i),
                [self.analytics_data[cat][i] for cat in chart_cats],
                width, label=reg,
                color=reg_colors[i % len(reg_colors)],
                edgecolor="black", zorder=3,
            )

        ww_vals = [self.analytics_data[cat][ww_idx] for cat in chart_cats]
        line_x  = x + width * ((len(regions_no_ww) - 1) / 2)
        ax1.plot(
            line_x, ww_vals, color="#C65911",
            marker="o", linewidth=2, label="Worldwide", zorder=4,
        )
        for i, val in enumerate(ww_vals):
            ax1.text(
                line_x[i], val + 3, f"{val:.1f}%",
                ha="center", va="bottom", color="white",
                fontweight="bold", fontsize=9,
            )

        ax1.set_title(f"{self.team_name} Team", fontsize=12, fontweight="bold", color="white", pad=15)
        ax1.set_ylabel("Course Completion", color="white", fontweight="bold")
        ax1.set_xticks(line_x)
        ax1.set_xticklabels(chart_cats)
        ax1.set_ylim(0, 105)
        ax1.yaxis.set_major_formatter(plt.FuncFormatter(lambda y, _: f"{y:.0f}%"))
        ax1.tick_params(colors="white")
        ax1.legend(loc="center left", bbox_to_anchor=(1.02, 0.5), frameon=False, labelcolor="white")

        ax2 = self.figure.add_subplot(212)
        ax2.set_facecolor("#121214")
        ax2.grid(axis="y", linestyle="-", color="#3a3a40", alpha=0.7, zorder=0)
        x2, width2 = np.arange(len(self.regions)), 0.12
        cat_colors = ["#00B050", "#7030A0", "#C65911", "#7F7F7F", "#4472C4", "#548235", "#000000"]

        for i, cat in enumerate(chart_cats):
            ax2.bar(
                x2 + (width2 * i),
                self.analytics_data[cat],
                width2, label=cat,
                color=cat_colors[i % len(cat_colors)],
                edgecolor="black", zorder=3,
            )

        center_x2 = x2 + width2 * ((len(chart_cats) - 1) / 2)
        ax2.set_title(f"{self.team_name} Team", fontsize=12, fontweight="bold", color="white", pad=15)
        ax2.set_ylabel("Course Completion", color="white", fontweight="bold")
        ax2.set_xticks(center_x2)
        ax2.set_xticklabels(self.regions)
        ax2.set_ylim(0, 105)
        ax2.yaxis.set_major_formatter(plt.FuncFormatter(lambda y, _: f"{y:.0f}%"))
        ax2.tick_params(colors="white")
        ax2.legend(loc="center left", bbox_to_anchor=(1.02, 0.5), frameon=False, labelcolor="white")

        self.figure.tight_layout(rect=[0, 0, 0.88, 1])
        self.canvas.draw()

    def export_analytics(self):
        path, _ = QFileDialog.getSaveFileName(
            self.main_ui, "Export Analytics",
            f"{self.team_name}_Analytics_Export.xlsx", "Excel Files (*.xlsx)",
        )
        if not path:
            return

        QApplication.setOverrideCursor(Qt.WaitCursor)
        try:
            # 1. Pull data directly from the currently viewed table
            data = []
            headers = [self.table.horizontalHeaderItem(i).text() for i in range(self.table.columnCount())]
            
            for row in range(self.table.rowCount()):
                row_data = {}
                for col in range(self.table.columnCount()):
                    item = self.table.item(row, col)
                    val = item.text() if item else ""
                    
                    if "%" in val:
                        try:
                            row_data[headers[col]] = float(val.replace("%", "")) / 100.0
                        except:
                            row_data[headers[col]] = val
                    else:
                        row_data[headers[col]] = val
                data.append(row_data)

            df = pd.DataFrame(data)

            # Pre-render graph to a memory buffer
            img_data = io.BytesIO()
            self.figure.savefig(img_data, format="png", facecolor=self.figure.get_facecolor(), edgecolor='none', bbox_inches='tight')

            # 2. Apply pristine formatting and embed graphs
            try:
                # Attempt to use xlsxwriter for perfect image placement
                writer = pd.ExcelWriter(path, engine="xlsxwriter")
                df.to_excel(writer, sheet_name="Analytics", index=False)
                worksheet = writer.sheets["Analytics"]

                pct_format = writer.book.add_format({"num_format": "0.00%"})
                worksheet.set_column(1, len(headers) - 1, 15, pct_format)
                worksheet.set_column(0, 0, 25)

                header_format = writer.book.add_format({'bold': True, 'bg_color': '#202025', 'font_color': 'white', 'border': 1})
                for col_num, value in enumerate(df.columns.values):
                    worksheet.write(0, col_num, value, header_format)

                img_data.seek(0)
                # Scale slightly so it's not gigantic in the Excel file
                worksheet.insert_image(len(df) + 3, 0, "chart.png", {"image_data": img_data, "x_scale": 0.7, "y_scale": 0.7})
                writer.close()

            except ImportError:
                # Fallback: xlsxwriter missing, use openpyxl standard export
                df.to_excel(path, sheet_name="Analytics", index=False)
                
                graph_added = False
                try:
                    import openpyxl
                    from openpyxl.drawing.image import Image as xlImage
                    
                    wb = openpyxl.load_workbook(path)
                    ws = wb["Analytics"]
                    
                    img_data.seek(0)
                    xl_img = xlImage(img_data)
                    xl_img.width = int(xl_img.width * 0.7)
                    xl_img.height = int(xl_img.height * 0.7)
                    
                    ws.add_image(xl_img, f"A{len(df) + 4}")
                    wb.save(path)
                    graph_added = True
                except Exception as inner_exc:
                    print(f"[ERROR] Openpyxl image fallback failed: {inner_exc}")
                
                if not graph_added:
                    QMessageBox.warning(
                        self.main_ui, "Missing Library",
                        "The table exported successfully, but the graph was skipped.\n\n"
                        "To include graphs in the Excel export, please ensure the 'xlsxwriter' library is installed:\n"
                        "pip install xlsxwriter"
                    )

            QMessageBox.information(
                self.main_ui, "Export Successful",
                f"{self.team_name} Analytics exported successfully to:\n{path}",
            ) 

        except Exception as exc:
            QMessageBox.critical(self.main_ui, "Export Error", f"Failed to export analytics:\n{exc}")
        finally:
            QApplication.restoreOverrideCursor()


# ── Summary Analytics Controller (Current + Trend) ────────────────────────────

class SummaryAnalyticsController:
    """Controller linking UI elements for the Summary (Current Month and Yearly Trends)."""

    def __init__(self, main_ui):
        self.ui = main_ui
        
        self.table_current = main_ui.table_summary_current
        self.scroll_current = main_ui.scroll_summary_current
        self.table_trend = main_ui.table_summary_trend
        self.scroll_trend = main_ui.scroll_summary_trend
        
        self.btn_export = main_ui.btn_export_current_summary
        self.btn_load = main_ui.btn_load_trend
        
        self.btn_export.clicked.connect(self.export_current_summary)
        self.btn_load.clicked.connect(self.load_trend_folder)

        self.regions = ["Taiwan", "China", "Korea", "Japan", "US/Canada", "Europe", "ISEA", "Worldwide"]
        self.months  = ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"]
        self.teams   = ["FAE", "Sales", "FAE+Sales"]
        
        self.current_data = {}
        self.trend_data = {}
        
        self.is_loaded = False
        
        self.setup_ui_elements()

    def setup_ui_elements(self):
        self.fig_current = plt.figure(figsize=(11, 15))
        self.fig_current.patch.set_facecolor("#121214")
        self.canvas_current = FigureCanvas(self.fig_current)
        self.canvas_current.setStyleSheet("background-color: transparent;")
        
        self.canvas_current.setMinimumSize(1000, 1550)
        self.scroll_current.setWidget(self.canvas_current)
        
        self.fig_trend = plt.figure(figsize=(11, 15))
        self.fig_trend.patch.set_facecolor("#121214")
        self.canvas_trend = FigureCanvas(self.fig_trend)
        self.canvas_trend.setStyleSheet("background-color: transparent;")
        
        self.canvas_trend.setMinimumSize(1000, 1550)
        self.scroll_trend.setWidget(self.canvas_trend)

    def auto_load_data(self):
        if self.is_loaded:
            return
            
        try:
            self.calculate_current_summary_from_db()
            self.populate_current_table()
            self.draw_current_charts()
            
            if not self.trend_data:
                self.initialize_empty_trend_data()
                self.populate_trend_table()
                self.draw_trend_charts()
                
            self.is_loaded = True
        except Exception as exc:
            print(f"[ERROR] generating Current Summary: {exc}")

    def calculate_current_summary_from_db(self):
        self.current_data = {t: {"Published": 0, "Regions": {r: 0.0 for r in self.regions}} for t in self.teams}
        
        regions_db_map = {
            "Taiwan": "Taiwan", "China": "China", "Korea": "Korea", "Japan": "Japan",
            "US/Canada": "Americas", "Europe": "Europe", "ISEA": "ISEA", "Worldwide": "Worldwide"
        }

        with sqlite3.connect(DB_PATH) as conn:
            cursor = conn.cursor()
            
            cursor.execute("SELECT course_id, fae_control_code, sales_control_code, course_type FROM courses")
            courses = cursor.fetchall()
            
            fae_courses   = [str(r[0]) for r in courses if r[3] in ("FAE", "Both")]
            sales_courses = [str(r[0]) for r in courses if r[3] in ("Sales", "Both")]
            both_courses  = list(set(fae_courses + sales_courses))

            cursor.execute(
                "SELECT user_id, organization, region_bucket, control_codes "
                "FROM students WHERE organization IN ('FAE', 'Sales')"
            )
            students = cursor.fetchall()

            cursor.execute(
                """
                SELECT c.user_id, c.course_id, c.percent_complete
                FROM completions c JOIN students s ON s.user_id = c.user_id
                WHERE s.organization IN ('FAE', 'Sales')
                """
            )
            completions_map = {}
            for uid, cid, pct in cursor.fetchall():
                uid_str = normalize_uid(uid)
                completions_map.setdefault(uid_str, {})[str(cid)] = safe_float(pct)

        teams_config = {
            "FAE":       {"orgs": ["FAE"],          "pub": len(fae_courses)},
            "Sales":     {"orgs": ["Sales"],        "pub": len(sales_courses)},
            "FAE+Sales": {"orgs": ["FAE", "Sales"], "pub": len(both_courses)},
        }

        for team, t_info in teams_config.items():
            self.current_data[team]["Published"] = t_info["pub"]
            region_totals = {r: [] for r in regions_db_map}

            for st in students:
                user_id, org, region, control_codes = st
                if org not in t_info["orgs"]:
                    continue

                uid_str  = normalize_uid(user_id)
                my_codes = (
                    [c.strip() for c in str(control_codes).split(",")]
                    if control_codes and str(control_codes).lower() not in ("nan", "n/a", "none", "") else []
                )
                
                idx = 1 if org == 'FAE' else 2
                my_req_courses = [str(cid) for cid, fae_cc, sales_cc, ct in courses if (fae_cc if idx==1 else sales_cc) in my_codes]

                if my_req_courses:
                    total_pct = sum(completions_map.get(uid_str, {}).get(cid, 0.0) for cid in my_req_courses)
                    avg_pct = total_pct / len(my_req_courses)
                    
                    region_totals["Worldwide"].append(avg_pct)
                    for out_reg, db_reg in regions_db_map.items():
                        if out_reg != "Worldwide" and region == db_reg:
                            region_totals[out_reg].append(avg_pct)

            for r_name in regions_db_map:
                vals = region_totals[r_name]
                self.current_data[team]["Regions"][r_name] = (sum(vals) / len(vals)) if vals else 0.0

    def populate_current_table(self):
        self.table_current.setUpdatesEnabled(False)
        self.table_current.clear()

        headers = ["Team", "Published Courses"] + self.regions
        self.table_current.setColumnCount(len(headers))
        self.table_current.setHorizontalHeaderLabels(headers)
        self.table_current.setRowCount(len(self.teams))

        for r_idx, team in enumerate(self.teams):
            item_team = QTableWidgetItem(team)
            item_team.setTextAlignment(Qt.AlignCenter | Qt.AlignVCenter)
            font = item_team.font()
            font.setBold(True)
            item_team.setFont(font)
            self.table_current.setItem(r_idx, 0, item_team)

            pub_count = self.current_data[team]["Published"]
            item_pub = QTableWidgetItem(str(pub_count))
            item_pub.setTextAlignment(Qt.AlignCenter | Qt.AlignVCenter)
            self.table_current.setItem(r_idx, 1, item_pub)

            for c_idx, reg in enumerate(self.regions):
                val = self.current_data[team]["Regions"][reg]
                item_val = QTableWidgetItem(f"{val:.2f}%" if val > 0 else "0.00%")
                item_val.setTextAlignment(Qt.AlignCenter | Qt.AlignVCenter)
                if val == 0: item_val.setForeground(Qt.gray)
                self.table_current.setItem(r_idx, c_idx + 2, item_val)

        self.table_current.horizontalHeader().setMinimumSectionSize(110)
        self.table_current.horizontalHeader().setSectionResizeMode(0, QHeaderView.Stretch)
        for i in range(1, self.table_current.columnCount()):
            self.table_current.horizontalHeader().setSectionResizeMode(i, QHeaderView.Interactive)
            self.table_current.setColumnWidth(i, 110)

        self.table_current.setUpdatesEnabled(True)

    def draw_current_charts(self):
        plt.close(self.fig_current)
        self.fig_current = plt.figure(figsize=(11, 15))
        self.fig_current.patch.set_facecolor("#121214")
        self.canvas_current.figure = self.fig_current

        x = np.arange(len(self.regions))
        colors = ["#4472C4", "#ED7D31", "#A5A5A5", "#FFC000", "#5B9BD5", "#00B050", "#264478", "#C65911"]

        for i, team in enumerate(self.teams):
            ax = self.fig_current.add_subplot(3, 1, i + 1)
            ax.set_facecolor("#121214")
            ax.grid(axis="y", linestyle="-", color="#3a3a40", alpha=0.7, zorder=0)

            vals = [self.current_data[team]["Regions"][r] for r in self.regions]
            bars = ax.bar(x, vals, width=0.6, color=colors[:len(vals)], edgecolor="black", zorder=3)

            for bar in bars:
                yval = bar.get_height()
                if yval > 0:
                    ax.text(bar.get_x() + bar.get_width()/2, yval + 2, f"{yval:.1f}%", 
                            ha="center", va="bottom", color="white", fontweight="bold", fontsize=9)

            ax.set_title(f"{team} Team - Current Analytics", color="white", fontweight="bold", pad=15)
            ax.set_ylabel("Course Completion", color="white", fontweight="bold")
            ax.set_xticks(x)
            ax.set_xticklabels(self.regions, color="white")
            ax.set_ylim(0, 105)
            ax.yaxis.set_major_formatter(plt.FuncFormatter(lambda y, _: f"{y:.0f}%"))
            ax.tick_params(colors="white")

        self.fig_current.tight_layout(rect=[0, 0, 1, 1])
        self.canvas_current.draw()

    def export_current_summary(self):
        path, _ = QFileDialog.getSaveFileName(
            self.ui, "Export Current Month Summary",
            "01-JAN-SUMMARY.xlsx", "Excel Files (*.xlsx)"
        )
        if not path:
            return

        QApplication.setOverrideCursor(Qt.WaitCursor)
        try:
            # 1. Pull data directly from the currently viewed summary table
            data = []
            headers = [self.table_current.horizontalHeaderItem(i).text() for i in range(self.table_current.columnCount())]
            
            for row in range(self.table_current.rowCount()):
                row_data = {}
                for col in range(self.table_current.columnCount()):
                    item = self.table_current.item(row, col)
                    val = item.text() if item else ""
                    
                    if "%" in val:
                        try:
                            row_data[headers[col]] = float(val.replace("%", "")) / 100.0
                        except:
                            row_data[headers[col]] = val
                    else:
                        try:
                            # Catch "Published Courses" whole numbers
                            row_data[headers[col]] = int(val) if val.isdigit() else val
                        except:
                            row_data[headers[col]] = val
                data.append(row_data)

            df = pd.DataFrame(data)

            # Pre-render graph to a memory buffer
            img_data = io.BytesIO()
            self.fig_current.savefig(img_data, format="png", facecolor=self.fig_current.get_facecolor(), edgecolor='none', bbox_inches='tight')

            # 2. Apply pristine formatting and embed graphs
            try:
                writer = pd.ExcelWriter(path, engine="xlsxwriter")
                df.to_excel(writer, sheet_name="Summary", index=False)
                worksheet = writer.sheets["Summary"]
                
                pct_format = writer.book.add_format({"num_format": "0.00%"})
                worksheet.set_column(2, len(headers) - 1, 15, pct_format)
                worksheet.set_column(0, 1, 20)

                header_format = writer.book.add_format({'bold': True, 'bg_color': '#202025', 'font_color': 'white', 'border': 1})
                for col_num, value in enumerate(df.columns.values):
                    worksheet.write(0, col_num, value, header_format)

                img_data.seek(0)
                # Scale slightly so it's not gigantic in the Excel file
                worksheet.insert_image(len(df) + 3, 0, "chart.png", {"image_data": img_data, "x_scale": 0.7, "y_scale": 0.7})
                writer.close()

            except ImportError:
                # Fallback: xlsxwriter missing, use openpyxl standard export
                df.to_excel(path, sheet_name="Summary", index=False)
                
                graph_added = False
                try:
                    import openpyxl
                    from openpyxl.drawing.image import Image as xlImage
                    
                    wb = openpyxl.load_workbook(path)
                    ws = wb["Summary"]
                    
                    img_data.seek(0)
                    xl_img = xlImage(img_data)
                    xl_img.width = int(xl_img.width * 0.7)
                    xl_img.height = int(xl_img.height * 0.7)
                    
                    ws.add_image(xl_img, f"A{len(df) + 4}")
                    wb.save(path)
                    graph_added = True
                except Exception as inner_exc:
                    print(f"[ERROR] Openpyxl image fallback failed: {inner_exc}")
                
                if not graph_added:
                    QMessageBox.warning(
                        self.ui, "Missing Library",
                        "The summary table exported successfully, but the graph was skipped.\n\n"
                        "To include graphs in the Excel export, please ensure the 'xlsxwriter' library is installed:\n"
                        "pip install xlsxwriter"
                    )

            QMessageBox.information(self.ui, "Export Successful", f"Summary successfully saved to:\n{path}")
        except Exception as exc:
            QMessageBox.critical(self.ui, "Export Error", f"Failed to export:\n{exc}")
        finally:
            QApplication.restoreOverrideCursor()

    # --- Trend Folder Logic ---

    def initialize_empty_trend_data(self):
        self.trend_data = {
            team: {"published": [np.nan]*12, "regions": {r: [np.nan]*12 for r in self.regions}}
            for team in self.teams
        }

    def load_trend_folder(self):
        folder_path = QFileDialog.getExistingDirectory(self.ui, "Select Folder ")
        if not folder_path:
            return

        QApplication.setOverrideCursor(Qt.WaitCursor)
        try:
            self.initialize_empty_trend_data()
            
            files = glob.glob(os.path.join(folder_path, "*.xlsx"))
            files += glob.glob(os.path.join(folder_path, "*.xls"))

            parsed_count = 0
            for file in files:
                filename = os.path.basename(file).upper()
                
                if filename.startswith("~$"):
                    continue
                
                match = re.search(r'^(0[1-9]|1[0-2])[-_]', filename)
                month_idx = -1
                
                if match:
                    month_idx = int(match.group(1)) - 1
                else:
                    for i, m in enumerate(self.months):
                        if m.upper() in filename:
                            month_idx = i
                            break
                            
                if month_idx == -1:
                    continue

                df = pd.read_excel(file)
                for _, row in df.iterrows():
                    team = row.get("Team")
                    if team in self.teams:
                        pub = safe_float(row.get("Published Courses", 0))
                        self.trend_data[team]["published"][month_idx] = pub
                        for reg in self.regions:
                            val = row.get(reg, np.nan)
                            if pd.notna(val):
                                if isinstance(val, str) and '%' in val:
                                    val = safe_float(val.replace('%', ''))
                                elif isinstance(val, (int, float)) and 0.0 < val <= 1.0:
                                    val = val * 100
                                self.trend_data[team]["regions"][reg][month_idx] = float(val)
                parsed_count += 1

            self.populate_trend_table()
            self.draw_trend_charts()
            
            QMessageBox.information(self.ui, "Folder Loaded", f"Successfully parsed {parsed_count} monthly summary files.")
        except Exception as exc:
            QMessageBox.critical(self.ui, "Parse Error", f"Failed to load trend data:\n{exc}")
        finally:
            QApplication.restoreOverrideCursor()

    def populate_trend_table(self):
        self.table_trend.setUpdatesEnabled(False)
        self.table_trend.clear()

        headers = ["Team", "Metric"] + self.months
        self.table_trend.setColumnCount(len(headers))
        self.table_trend.setHorizontalHeaderLabels(headers)

        rows_data = []
        for team in self.teams:
            pub_row = [team, "Published Courses"]
            for m in range(12):
                val = self.trend_data[team]["published"][m]
                pub_row.append(str(int(val)) if pd.notna(val) else "-")
            rows_data.append(pub_row)
            
            for reg in self.regions:
                reg_row = [team, reg]
                for m in range(12):
                    val = self.trend_data[team]["regions"][reg][m]
                    reg_row.append(f"{val:.2f}%" if pd.notna(val) else "-")
                rows_data.append(reg_row)

        self.table_trend.setRowCount(len(rows_data))
        for r_idx, row in enumerate(rows_data):
            for c_idx, val in enumerate(row):
                item = QTableWidgetItem(val)
                item.setTextAlignment(Qt.AlignCenter | Qt.AlignVCenter)
                if c_idx < 2:
                    font = item.font()
                    font.setBold(True)
                    item.setFont(font)
                if val == "-":
                    item.setForeground(Qt.gray)
                self.table_trend.setItem(r_idx, c_idx, item)

        self.table_trend.horizontalHeader().setMinimumSectionSize(60)
        self.table_trend.horizontalHeader().setSectionResizeMode(0, QHeaderView.ResizeToContents)
        self.table_trend.horizontalHeader().setSectionResizeMode(1, QHeaderView.ResizeToContents)
        for i in range(2, self.table_trend.columnCount()):
            self.table_trend.horizontalHeader().setSectionResizeMode(i, QHeaderView.Stretch)

        self.table_trend.setUpdatesEnabled(True)

    def draw_trend_charts(self):
        plt.close(self.fig_trend)
        self.fig_trend = plt.figure(figsize=(11, 15))
        self.fig_trend.patch.set_facecolor("#121214")
        self.canvas_trend.figure = self.fig_trend

        for i, team in enumerate(self.teams):
            data = self.trend_data.get(team)
            if not data:
                continue

            ax1 = self.fig_trend.add_subplot(3, 1, i + 1)
            ax1.set_facecolor("#121214")
            ax2 = ax1.twinx()
            x = np.arange(12)

            bars = ax2.bar(
                x, data["published"], width=0.25,
                color="#B4C6E7", edgecolor="black", zorder=1,
            )
            bar_label_col = "#00B050" if team == "FAE+Sales" else "red"
            for bar in bars:
                yval = bar.get_height()
                if pd.notna(yval) and yval > 0:
                    ax2.text(bar.get_x() + bar.get_width() / 2, yval + 2, str(int(yval)),
                             ha="center", va="bottom", color=bar_label_col, fontweight="bold", fontsize=9)

            reg_colors = ["#ED7D31", "#FF0000", "#FFC000", "#5B9BD5", "#70AD47", "#264478", "#9E480E", "#7F7F7F"]
            for j, reg in enumerate(self.regions):
                ax1.plot(
                    x, data["regions"][reg],
                    label=reg, color=reg_colors[j],
                    marker="o", markersize=6, linewidth=2, zorder=3,
                )

            ax1.set_title(f"{team} Team Yearly Trend", color="white", fontweight="bold", pad=10)
            ax1.set_xticks(x)
            ax1.set_xticklabels(self.months, color="white")
            ax1.set_ylim(0, 120)
            ax1.yaxis.set_major_formatter(plt.FuncFormatter(lambda y, _: f"{y:.0f}%"))
            ax1.set_ylabel("Course Completion Percentage", color="white")
            ax1.tick_params(colors="white")
            ax2.set_ylim(0, 100)
            ax2.set_ylabel("Course Published", color=bar_label_col, fontweight="bold")
            ax2.tick_params(axis="y", colors=bar_label_col)
            ax1.grid(True, axis="both", linestyle="-", color="#3a3a40", alpha=0.7)

            if i == 0:
                bar_patch = mpatches.Patch(color="#B4C6E7", ec="black", label="Published\nCourse")
                handles, labels = ax1.get_legend_handles_labels()
                ax1.legend(
                    handles=[bar_patch] + handles, labels=[bar_patch.get_label()] + labels,
                    loc="center left", bbox_to_anchor=(1.08, 0.5), frameon=False, labelcolor="white",
                )

        self.fig_trend.tight_layout(rect=[0, 0, 0.88, 1])
        self.canvas_trend.draw()


# ── Main Analytics Tab Component ──────────────────────────────────────────────

class AnalyticsTabComponent:
    def __init__(self, ui_window):
        self.ui = ui_window
        self.data_dirty = True
        plt.style.use("dark_background")

        self.controllers = {
            0: TeamAnalyticsController("FAE", self.ui.table_fae, self.ui.scroll_fae, self.ui.btn_export_fae, self.ui),
            1: TeamAnalyticsController("Sales", self.ui.table_sales, self.ui.scroll_sales, self.ui.btn_export_sales, self.ui),
            2: SummaryAnalyticsController(self.ui)
        }

        self.ui.tabWidget_analytics.tabBar().setExpanding(True)
        self.ui.tabWidget_analytics.currentChanged.connect(self.load_current_tab)
        
        self.ui.tab_fae.tabBar().setExpanding(True)
        self.ui.tab_sales.tabBar().setExpanding(True)
        self.ui.tab_summary.tabBar().setExpanding(True)
    
        self.ui.tabWidget_analytics.setStyleSheet("""
            QTabWidget#tabWidget_analytics  QTabBar::tab {
                background: #202025;
                color: #777777;
                border: 1px solid #3a3a40;
                padding: 8px 15px;
                font-size: 14px;
                min-height: 20px;
            }
            QTabWidget#tabWidget_analytics  QTabBar::tab:selected {
                background: #0085ca;
                color: white;
                border: 1px solid #0085ca;
                font-weight: bold;
            }
            QTabWidget#tabWidget_analytics  QTabBar::tab:hover:!selected {
                background: #2a2a30;
            }
        """)

    def update_data(self):
        if self.data_dirty:
            for c in self.controllers.values():
                c.is_loaded = False
            self.data_dirty = False
            
        self.load_current_tab()

    def load_current_tab(self):
        idx = self.ui.tabWidget_analytics.currentIndex()
        if idx in self.controllers:
            QApplication.setOverrideCursor(Qt.WaitCursor)
            try:
                self.controllers[idx].auto_load_data()
            finally:
                QApplication.restoreOverrideCursor()

    def mark_dirty(self):
        self.data_dirty = True
</file>

<file path="components/tab_matrix.py">
from PyQt5.QtWidgets import (
    QTableWidgetItem, QApplication, QDialog, QCheckBox,
    QMessageBox, QTableWidget, QFileDialog, QMenu, QWidgetAction,
    QTreeWidgetItem, QTableView, QHeaderView, QListWidgetItem,
    QInputDialog, QAbstractItemView, QTreeWidget, QListWidget
)
from PyQt5.QtGui import QColor, QBrush
from PyQt5.QtCore import Qt, QAbstractTableModel, QSortFilterProxyModel
from PyQt5 import uic
import sqlite3
import pandas as pd
import json
from database import DB_PATH
from data_manager import safe_float, normalize_uid
import os
import sys

def resource_path(relative_path):
    try:
        base_path = sys._MEIPASS
    except Exception:
        base_path = os.path.abspath(".")
    return os.path.join(base_path, relative_path)

# ── Course Mapping Dialog ──────────────────────────────────────────────────────

class CourseMappingDialog(QDialog):
    def __init__(self, current_org_filter, parent=None):
        super().__init__(parent)
        ui_path = resource_path("dialog_mapping.ui")
        uic.loadUi(ui_path, self)

        self.course_filter  = current_org_filter
        self.prefix = self.course_filter.lower()
        self.courses_deleted = False
        self.setWindowTitle(f"Map Courses to Control Codes ({self.course_filter} View)")

        self.tree_categories.dropEvent = self.tree_drop_event
        self.list_unassigned.dropEvent = self.list_drop_event
        
        self.tree_categories.setContextMenuPolicy(Qt.CustomContextMenu)
        self.tree_categories.customContextMenuRequested.connect(self.show_tree_context_menu)

        self.btn_save.clicked.connect(self.save_mapping)

        self.setup_tree_structure()
        self.load_data()

    def get_item_level(self, item):
        level = 0
        p = item.parent()
        while p:
            level += 1
            p = p.parent()
        return level

    def show_tree_context_menu(self, pos):
        item = self.tree_categories.itemAt(pos)
        menu = QMenu(self)
        menu.setStyleSheet("""
            QMenu { background: #202025; color: white; border: 1px solid #3a3a40; }
            QMenu::item:selected { background: #0085ca; }
        """)

        if item is None:
            action_add = menu.addAction("Add Category")
            action_add.triggered.connect(self.add_category)
        else:
            level = self.get_item_level(item)
            if level == 0:
                action_add_sub = menu.addAction("Add Sub-category")
                action_add_sub.triggered.connect(lambda: self.add_sub_category(item))
                action_rename = menu.addAction("Rename Category")
                action_rename.triggered.connect(lambda: self.rename_item(item, level))
                action_del = menu.addAction("Delete Category")
                action_del.triggered.connect(lambda: self.delete_item(item, level))
            elif level == 1:
                action_rename = menu.addAction("Rename Sub-category")
                action_rename.triggered.connect(lambda: self.rename_item(item, level))
                action_del = menu.addAction("Delete Sub-category")
                action_del.triggered.connect(lambda: self.delete_item(item, level))
            elif level >= 2:
                action_unassign = menu.addAction("Remove from Category")
                action_unassign.triggered.connect(lambda: self.unassign_course(item))
                action_delete = menu.addAction("Delete Course completely")
                action_delete.triggered.connect(lambda: self.delete_course_completely(item))

        menu.exec_(self.tree_categories.viewport().mapToGlobal(pos))

    def add_category(self):
        name, ok = QInputDialog.getText(self, "Add Category", "Enter new Category name:")
        if ok and name.strip():
            node = QTreeWidgetItem(self.tree_categories, [name.strip()])
            node.setFlags(Qt.ItemIsEnabled | Qt.ItemIsSelectable | Qt.ItemIsDragEnabled | Qt.ItemIsDropEnabled)

    def add_sub_category(self, parent_item):
        name, ok = QInputDialog.getText(self, "Add Sub-category", "Enter new Sub-category name:")
        if ok and name.strip():
            node = QTreeWidgetItem(parent_item, [name.strip()])
            node.setFlags(Qt.ItemIsEnabled | Qt.ItemIsSelectable | Qt.ItemIsDragEnabled | Qt.ItemIsDropEnabled)
            parent_item.setExpanded(True)

    def rename_item(self, item, level):
        old_name = item.text(0)
        new_name, ok = QInputDialog.getText(self, "Rename", f"Rename '{old_name}' to:", text=old_name)
        if ok and new_name.strip() and new_name.strip() != old_name:
            new_name = new_name.strip()
            
            reply = QMessageBox.question(
                self, "Confirm Rename",
                f"Renaming this will globally update all related courses and student assignments in the database.\n\nContinue?",
                QMessageBox.Yes | QMessageBox.No
            )
            
            if reply == QMessageBox.Yes:
                item.setText(0, new_name)
                with sqlite3.connect(DB_PATH) as conn:
                    cursor = conn.cursor()
                    if level == 0:
                        cursor.execute("UPDATE courses SET fae_control_code = ? WHERE fae_control_code = ?", (new_name, old_name))
                        cursor.execute("UPDATE courses SET sales_control_code = ? WHERE sales_control_code = ?", (new_name, old_name))
                        cursor.execute("UPDATE courses SET unknown_control_code = ? WHERE unknown_control_code = ?", (new_name, old_name))
                        
                        cursor.execute("SELECT user_id, control_codes FROM students")
                        records = []
                        for uid, codes in cursor.fetchall():
                            if codes:
                                code_list = [c.strip() for c in codes.split(",")]
                                if old_name in code_list:
                                    code_list = [new_name if c == old_name else c for c in code_list]
                                    records.append((", ".join(code_list), uid))
                        if records:
                            cursor.executemany("UPDATE students SET control_codes = ? WHERE user_id = ?", records)
                    
                    elif level == 1:
                        cursor.execute("UPDATE courses SET fae_graph_category = ? WHERE fae_graph_category = ?", (new_name, old_name))
                        cursor.execute("UPDATE courses SET sales_graph_category = ? WHERE sales_graph_category = ?", (new_name, old_name))
                        cursor.execute("UPDATE courses SET unknown_graph_category = ? WHERE unknown_graph_category = ?", (new_name, old_name))

    def delete_item(self, item, level):
        reply = QMessageBox.question(
            self, "Confirm Delete",
            f"Deleting '{item.text(0)}' will move all its mapped courses to the Unassigned list in THIS view.\n\nContinue?",
            QMessageBox.Yes | QMessageBox.No
        )
        if reply == QMessageBox.Yes:
            courses_to_move = []
            def extract_courses(node):
                if node.childCount() == 0 and node.parent() and node.parent().parent():
                    courses_to_move.append(node)
                for i in range(node.childCount()):
                    extract_courses(node.child(i))
                    
            extract_courses(item)
            
            for course_node in courses_to_move:
                list_item = QListWidgetItem(course_node.text(0))
                list_item.setData(Qt.UserRole, course_node.data(0, Qt.UserRole))
                self.list_unassigned.addItem(list_item)
                
            if item.parent():
                item.parent().removeChild(item)
            else:
                self.tree_categories.takeTopLevelItem(self.tree_categories.indexOfTopLevelItem(item))

    def unassign_course(self, item):
        list_item = QListWidgetItem(item.text(0))
        list_item.setData(Qt.UserRole, item.data(0, Qt.UserRole))
        self.list_unassigned.addItem(list_item)
        item.parent().removeChild(item)

    def delete_course_completely(self, item):
        reply = QMessageBox.question(
            self, "Confirm Permanent Delete",
            f"Are you sure you want to PERMANENTLY delete '{item.text(0)}' from the database?\nThis will erase all completion records for this course globally.",
            QMessageBox.Yes | QMessageBox.No
        )
        if reply == QMessageBox.Yes:
            course_id = item.data(0, Qt.UserRole)
            with sqlite3.connect(DB_PATH) as conn:
                conn.execute("DELETE FROM courses WHERE course_id=?", (course_id,))
                conn.execute("DELETE FROM completions WHERE course_id=?", (course_id,))
            item.parent().removeChild(item)
            self.courses_deleted = True

    def tree_drop_event(self, event):
        source = event.source()
        target_item = self.tree_categories.itemAt(event.pos())
        drop_indicator = self.tree_categories.dropIndicatorPosition()

        if source == self.tree_categories:
            dragged_items = self.tree_categories.selectedItems()
            if not dragged_items:
                event.ignore()
                return
            
            dragged_item = dragged_items[0]
            drag_level = self.get_item_level(dragged_item)

            if not target_item:
                if drag_level == 0:
                    idx = self.tree_categories.indexOfTopLevelItem(dragged_item)
                    item = self.tree_categories.takeTopLevelItem(idx)
                    self.tree_categories.addTopLevelItem(item)
                    event.setDropAction(Qt.MoveAction)
                    event.accept()
                else:
                    event.ignore()
                return

            target_level = self.get_item_level(target_item)

            if drag_level == 0:
                if target_level == 0 and drop_indicator != QAbstractItemView.OnItem:
                    curr_idx = self.tree_categories.indexOfTopLevelItem(dragged_item)
                    target_idx = self.tree_categories.indexOfTopLevelItem(target_item)
                    
                    if drop_indicator == QAbstractItemView.BelowItem:
                        target_idx += 1
                    if curr_idx < target_idx:
                        target_idx -= 1 
                        
                    item = self.tree_categories.takeTopLevelItem(curr_idx)
                    self.tree_categories.insertTopLevelItem(target_idx, item)
                    event.setDropAction(Qt.MoveAction)
                    event.accept()
                else:
                    event.ignore()
                return

            elif drag_level == 1:
                if target_level == 0 and drop_indicator == QAbstractItemView.OnItem:
                    dragged_item.parent().removeChild(dragged_item)
                    target_item.addChild(dragged_item)
                    target_item.setExpanded(True)
                    event.setDropAction(Qt.MoveAction)
                    event.accept()
                elif target_level == 1 and drop_indicator != QAbstractItemView.OnItem:
                    parent = target_item.parent()
                    curr_parent = dragged_item.parent()
                    
                    target_idx = parent.indexOfChild(target_item)
                    if drop_indicator == QAbstractItemView.BelowItem:
                        target_idx += 1
                        
                    if curr_parent == parent:
                        curr_idx = parent.indexOfChild(dragged_item)
                        if curr_idx < target_idx:
                            target_idx -= 1 
                            
                    curr_parent.removeChild(dragged_item)
                    parent.insertChild(target_idx, dragged_item)
                    event.setDropAction(Qt.MoveAction)
                    event.accept()
                else:
                    event.ignore()
                return

            elif drag_level == 2:
                if target_level == 1 and drop_indicator == QAbstractItemView.OnItem:
                    dragged_item.parent().removeChild(dragged_item)
                    target_item.addChild(dragged_item)
                    target_item.setExpanded(True)
                    event.setDropAction(Qt.MoveAction)
                    event.accept()
                elif target_level == 2 and drop_indicator != QAbstractItemView.OnItem:
                    parent = target_item.parent()
                    curr_parent = dragged_item.parent()
                    
                    target_idx = parent.indexOfChild(target_item)
                    if drop_indicator == QAbstractItemView.BelowItem:
                        target_idx += 1
                        
                    if curr_parent == parent:
                        curr_idx = parent.indexOfChild(dragged_item)
                        if curr_idx < target_idx:
                            target_idx -= 1 
                            
                    curr_parent.removeChild(dragged_item)
                    parent.insertChild(target_idx, dragged_item)
                    event.setDropAction(Qt.MoveAction)
                    event.accept()
                else:
                    event.ignore()
                return

        elif source == self.list_unassigned:
            if not target_item:
                event.ignore()
                return
                
            target_level = self.get_item_level(target_item)
            
            parent_sub = None
            insert_idx = -1
            
            if target_level == 1:
                if drop_indicator == QAbstractItemView.OnItem:
                    parent_sub = target_item
                    insert_idx = parent_sub.childCount()
                else:
                    event.ignore()
                    return
            elif target_level == 2:
                if drop_indicator != QAbstractItemView.OnItem:
                    parent_sub = target_item.parent()
                    insert_idx = parent_sub.indexOfChild(target_item)
                    if drop_indicator == QAbstractItemView.BelowItem:
                        insert_idx += 1
                else:
                    event.ignore()
                    return
            else:
                event.ignore()
                return

            for item in source.selectedItems():
                course_id = item.data(Qt.UserRole)
                new_item = QTreeWidgetItem([item.text()])
                new_item.setData(0, Qt.UserRole, course_id)
                new_item.setFlags(Qt.ItemIsEnabled | Qt.ItemIsSelectable | Qt.ItemIsDragEnabled | Qt.ItemIsDropEnabled)
                
                parent_sub.insertChild(insert_idx, new_item)
                insert_idx += 1 
                
                row = self.list_unassigned.row(item)
                self.list_unassigned.takeItem(row)
                
            parent_sub.setExpanded(True)
            event.setDropAction(Qt.CopyAction)
            event.accept()
        else:
            event.ignore()

    def list_drop_event(self, event):
        source = event.source()
        if source == self.list_unassigned:
            QListWidget.dropEvent(self.list_unassigned, event)
            event.accept()
        elif source == self.tree_categories:
            for item in source.selectedItems():
                level = self.get_item_level(item)
                if level == 2: 
                    course_id = item.data(0, Qt.UserRole)
                    new_item = QListWidgetItem(item.text(0))
                    new_item.setData(Qt.UserRole, course_id)
                    self.list_unassigned.addItem(new_item)
                    item.parent().removeChild(item)
            event.setDropAction(Qt.CopyAction)
            event.accept()
        else:
            event.ignore()

    def setup_tree_structure(self):
        structure = {}
        with sqlite3.connect(DB_PATH) as conn:
            cursor = conn.cursor()
            cursor.execute("SELECT value FROM app_settings WHERE key = 'category_tree'")
            row = cursor.fetchone()
            if row:
                structure = json.loads(row[0])
                
        for control_code, categories in structure.items():
            cc_node = QTreeWidgetItem(self.tree_categories, [control_code])
            cc_node.setFlags(Qt.ItemIsEnabled | Qt.ItemIsSelectable | Qt.ItemIsDragEnabled | Qt.ItemIsDropEnabled)
            for cat in categories:
                cat_node = QTreeWidgetItem(cc_node, [cat])
                cat_node.setFlags(Qt.ItemIsEnabled | Qt.ItemIsSelectable | Qt.ItemIsDragEnabled | Qt.ItemIsDropEnabled)
        self.tree_categories.expandAll()

    def load_data(self):
        with sqlite3.connect(DB_PATH) as conn:
            cursor = conn.cursor()
            query = f"""
                SELECT course_id, course_name, {self.prefix}_control_code, {self.prefix}_graph_category, {self.prefix}_sort_order 
                FROM courses WHERE course_type IN (?, 'Both')
                ORDER BY {self.prefix}_sort_order ASC, course_name ASC
            """
            cursor.execute(query, (self.course_filter,))

            for cid, cname, cc, gc, sort_order in cursor.fetchall():
                cname_lower = cname.lower()
                if self.course_filter == "FAE" and "sales" in cname_lower: continue
                if self.course_filter == "Sales" and "fae" in cname_lower: continue

                item_text = cname if cname else f"Course {cid}"
                
                if cc == "Unassigned" or gc == "Unassigned":
                    list_item = QListWidgetItem(item_text)
                    list_item.setData(Qt.UserRole, cid)
                    self.list_unassigned.addItem(list_item)
                else:
                    found = False
                    for i in range(self.tree_categories.topLevelItemCount()):
                        parent = self.tree_categories.topLevelItem(i)
                        if parent.text(0) == cc:
                            for j in range(parent.childCount()):
                                child = parent.child(j)
                                if child.text(0) == gc:
                                    course_node = QTreeWidgetItem(child, [item_text])
                                    course_node.setData(0, Qt.UserRole, cid)
                                    course_node.setFlags(Qt.ItemIsEnabled | Qt.ItemIsSelectable | Qt.ItemIsDragEnabled | Qt.ItemIsDropEnabled)
                                    found = True
                                    break
                        if found: break
                    
                    if not found:
                        list_item = QListWidgetItem(item_text)
                        list_item.setData(Qt.UserRole, cid)
                        self.list_unassigned.addItem(list_item)

    def save_mapping(self):
        tree_data = {}
        for i in range(self.tree_categories.topLevelItemCount()):
            cc_node = self.tree_categories.topLevelItem(i)
            cc_name = cc_node.text(0)
            tree_data[cc_name] = []
            for j in range(cc_node.childCount()):
                gc_node = cc_node.child(j)
                tree_data[cc_name].append(gc_node.text(0))

        records_to_update = []
        managed_course_ids = []

        for i in range(self.tree_categories.topLevelItemCount()):
            cc_node = self.tree_categories.topLevelItem(i)
            for j in range(cc_node.childCount()):
                gc_node = cc_node.child(j)
                for k in range(gc_node.childCount()):
                    course_node = gc_node.child(k)
                    c_id = course_node.data(0, Qt.UserRole)
                    records_to_update.append((cc_node.text(0), gc_node.text(0), k, c_id))
                    managed_course_ids.append(c_id)

        for row in range(self.list_unassigned.count()):
            item = self.list_unassigned.item(row)
            c_id = item.data(Qt.UserRole)
            managed_course_ids.append(c_id)

        with sqlite3.connect(DB_PATH) as conn:
            cursor = conn.cursor()
            
            cursor.execute("UPDATE app_settings SET value = ? WHERE key = 'category_tree'", (json.dumps(tree_data),))

            if managed_course_ids:
                placeholders = ",".join("?" * len(managed_course_ids))
                cursor.execute(
                    f"UPDATE courses SET {self.prefix}_control_code='Unassigned', {self.prefix}_graph_category='Unassigned', {self.prefix}_sort_order=999 WHERE course_id IN ({placeholders})",
                    managed_course_ids
                )

            if records_to_update:
                conn.executemany(
                    f"UPDATE courses SET {self.prefix}_control_code=?, {self.prefix}_graph_category=?, {self.prefix}_sort_order=? WHERE course_id=?",
                    records_to_update,
                )
                
        self.accept()


# ── Models for UI Virtualization ───────────────────────────────────────────────

class MatrixTableModel(QAbstractTableModel):
    def __init__(self, parent=None):
        super().__init__(parent)
        self._data = []
        
        self.color_green  = QColor(0, 176, 80)
        self.color_yellow = QColor(255, 255, 0)
        self.color_red    = QColor(255, 0, 0)
        self.color_gray   = QColor(100, 100, 100)
        self.bg_color_table = QColor("#121214")

    def rowCount(self, parent=None): 
        return len(self._data)
        
    def columnCount(self, parent=None): 
        return len(self._data[0]) if self._data else 0

    def data(self, index, role=Qt.DisplayRole):
        if not index.isValid(): 
            return None
            
        row, col = index.row(), index.column()
        cell = self._data[row][col]

        if role == Qt.DisplayRole:
            return str(cell["text"])
        elif role == Qt.TextAlignmentRole:
            return Qt.AlignCenter | Qt.AlignVCenter if col >= 1 else Qt.AlignLeft | Qt.AlignVCenter
        elif role == Qt.BackgroundRole:
            if col >= 5 and cell.get("bg"): return QBrush(cell["bg"])
            return QBrush(self.bg_color_table)
        elif role == Qt.ForegroundRole:
            if col >= 5 and cell.get("fg"): return QBrush(cell["fg"])
            return QBrush(Qt.white)
        elif role == Qt.UserRole: 
            if col == 0: return cell.get("codes", "")
            if col == 2: return cell.get("region", "")
            
        return None

    def update_data(self, new_data):
        self.beginResetModel()
        self._data = new_data
        self.endResetModel()


class MatrixFilterProxyModel(QSortFilterProxyModel):
    def __init__(self, parent=None):
        super().__init__(parent)
        self.search_txt = ""
        self.target_region = "All Regions"
        self.target_code = "All Codes"

    def set_filters(self, search, region, code):
        self.search_txt = search.lower()
        self.target_region = region
        self.target_code = code
        self.invalidateFilter()

    def filterAcceptsRow(self, source_row, source_parent):
        model = self.sourceModel()
        
        name = model.data(model.index(source_row, 0, source_parent)).lower()
        email = model.data(model.index(source_row, 1, source_parent)).lower()
        region = model.data(model.index(source_row, 2, source_parent), Qt.UserRole)
        codes = model.data(model.index(source_row, 0, source_parent), Qt.UserRole)

        match_search = (self.search_txt in name) or (self.search_txt in email)
        match_region = (self.target_region == "All Regions" or region == self.target_region)
        match_code   = (self.target_code == "All Codes" or self.target_code in codes)

        return match_search and match_region and match_code


# ── Matrix tab component ───────────────────────────────────────────────────────

class MatrixTabComponent:
    def __init__(self, ui_window):
        self.ui                 = ui_window
        self.current_org_filter = "FAE"

        self.ui.tabBar_matrix.addTab("FAE")
        self.ui.tabBar_matrix.addTab("Sales")
        self.ui.tabBar_matrix.addTab("Unknown")

        self.ui.btn_refresh_matrix.clicked.connect(self.open_mapping_manager)
        self.ui.btn_clear_matrix.clicked.connect(self.export_matrix_to_excel)

        table_widget = self.ui.table_matrix
        layout = self.ui.layout_matrix_tables  
        idx = layout.indexOf(table_widget)
        
        self.table_view = QTableView()
        self.table_view.setObjectName("table_matrix")
        self.table_view.horizontalHeader().setVisible(False)
        self.table_view.verticalHeader().setFixedWidth(50)
        
        new_style = self.ui.styleSheet().replace('QTableWidget {', 'QTableWidget, QTableView {')
        self.ui.setStyleSheet(new_style)
        
        layout.insertWidget(idx, self.table_view)
        table_widget.deleteLater()
        self.ui.table_matrix = self.table_view

        self.model = MatrixTableModel(self.ui)
        self.proxy = MatrixFilterProxyModel(self.ui)
        self.proxy.setSourceModel(self.model)
        self.ui.table_matrix.setModel(self.proxy)

        self.ui.header_table.verticalHeader().setFixedWidth(50)
        self.ui.header_table.setEditTriggers(QTableWidget.NoEditTriggers)
        self.ui.header_table.setVerticalScrollBarPolicy(Qt.ScrollBarAlwaysOff)
        self.ui.header_table.setHorizontalScrollBarPolicy(Qt.ScrollBarAlwaysOff)
        self.ui.header_table.setWordWrap(True)

        hh = self.ui.header_table.horizontalHeader()
        hh.setVisible(True)
        hh.setSectionResizeMode(QHeaderView.Interactive)
        hh.sectionResized.connect(
            lambda logicalIndex, oldSize, newSize: self.table_view.setColumnWidth(logicalIndex, newSize)
        )

        self.ui.table_matrix.horizontalScrollBar().valueChanged.connect(
            self.ui.header_table.horizontalScrollBar().setValue
        )
        self.ui.header_table.horizontalScrollBar().valueChanged.connect(
            self.ui.table_matrix.horizontalScrollBar().setValue
        )

        self.ui.header_table.setContextMenuPolicy(Qt.CustomContextMenu)
        self.ui.header_table.customContextMenuRequested.connect(
            self.show_column_menu
        )

        self.ui.tabBar_matrix.currentChanged.connect(self.on_tab_changed)

        self.setup_filters()
        self.load_matrix_data()

    def show_column_menu(self, pos):
        menu = QMenu(self.ui)
        menu.setStyleSheet(
            "QMenu { background: #202025; color: white; border: 1px solid #3a3a40; }"
        )

        for c in range(5):
            item = self.ui.header_table.item(0, c)
            text = item.text() if item else f"Column {c}"
            action = QWidgetAction(menu)
            chk    = QCheckBox(text)
            chk.setStyleSheet(
                "QCheckBox { padding: 5px; background: transparent; color: white; }"
                "QCheckBox:hover { background: #0085ca; }"
            )
            chk.setChecked(not self.ui.header_table.isColumnHidden(c))
            chk.toggled.connect(lambda checked, col=c: self.toggle_column(col, checked))
            action.setDefaultWidget(chk)
            menu.addAction(action)

        menu.addSeparator()
        courses_menu = menu.addMenu("Course Columns")
        courses_menu.setStyleSheet(
            "QMenu { background: #202025; color: white; border: 1px solid #3a3a40; }"
        )

        for c in range(5, self.ui.header_table.columnCount()):
            item = self.ui.header_table.item(2, c)
            if not item:
                continue
            action = QWidgetAction(courses_menu)
            chk    = QCheckBox(item.text())
            chk.setStyleSheet(
                "QCheckBox { padding: 5px; background: transparent; color: white; }"
                "QCheckBox:hover { background: #0085ca; }"
            )
            chk.setChecked(not self.ui.header_table.isColumnHidden(c))
            chk.toggled.connect(lambda checked, col=c: self.toggle_column(col, checked))
            action.setDefaultWidget(chk)
            courses_menu.addAction(action)

        menu.exec_(self.ui.header_table.viewport().mapToGlobal(pos))

    def toggle_column(self, col, show):
        self.ui.header_table.setColumnHidden(col, not show)
        self.ui.table_matrix.setColumnHidden(col, not show)

    def export_matrix_to_excel(self):
        path, _ = QFileDialog.getSaveFileName(
            self.ui, "Export Matrix", "Filtered_Matrix_Export.xlsx", "Excel Files (*.xlsx)"
        )
        if not path:
            return

        QApplication.setOverrideCursor(Qt.WaitCursor)
        try:
            cols = self.ui.header_table.columnCount()
            rows = self.proxy.rowCount()

            row0, row1, row2 = [], [], []
            curr_cc, curr_gc = "", ""

            # Extract Headers from UI
            for c in range(cols):
                if c < 5:
                    item = self.ui.header_table.item(0, c)
                    row0.append(item.text() if item else "")
                    row1.append("")
                    row2.append("")
                else:
                    if self.ui.header_table.item(0, c):
                        curr_cc = self.ui.header_table.item(0, c).text()
                    if self.ui.header_table.item(1, c):
                        curr_gc = self.ui.header_table.item(1, c).text()
                    row0.append(curr_cc)
                    row1.append(curr_gc)
                    item = self.ui.header_table.item(2, c)
                    row2.append(item.text() if item else "")

            # Extract Data from currently filtered view
            data = []
            for r in range(rows):
                row_data = []
                for c in range(cols):
                    idx = self.proxy.index(r, c)
                    val = self.proxy.data(idx, Qt.DisplayRole)
                    
                    # Convert completion strings to actual floats for Excel to format
                    if c >= 5 and val in ("1.00", "0.01", "0.00"):
                        row_data.append(float(val))
                    else:
                        row_data.append(val)
                data.append(row_data)

            # Create flat DataFrame (bypassing Pandas MultiIndex error)
            df = pd.DataFrame(data)

            try:
                # Use xlsxwriter for advanced layout
                writer = pd.ExcelWriter(path, engine="xlsxwriter")
                
                # Write data starting at row 3 (leaving rows 0, 1, 2 for our custom headers)
                df.to_excel(writer, sheet_name="Matrix", index=False, header=False, startrow=3)
                
                worksheet = writer.sheets["Matrix"]
                workbook = writer.book

                # Create beautiful UI-matching styles
                fmt_base_header = workbook.add_format({'bold': True, 'bg_color': '#202025', 'font_color': 'white', 'border': 1, 'align': 'center', 'valign': 'vcenter'})
                fmt_cc_header = workbook.add_format({'bold': True, 'bg_color': '#4472C4', 'font_color': 'white', 'border': 1, 'align': 'center', 'valign': 'vcenter'})
                fmt_gc_header = workbook.add_format({'bold': True, 'bg_color': '#666666', 'font_color': 'white', 'border': 1, 'align': 'center', 'valign': 'vcenter'})
                fmt_course_header = workbook.add_format({'bold': True, 'bg_color': '#2a2a30', 'font_color': 'white', 'border': 1, 'align': 'center', 'valign': 'vcenter', 'text_wrap': True})
                pct_format = workbook.add_format({"num_format": "0%"})

                # Manually write the 3-tiered headers
                for c in range(cols):
                    if c < 5:
                        # Merge the base headers (Name, Email, etc.) so they span all 3 header rows
                        worksheet.merge_range(0, c, 2, c, row0[c], fmt_base_header)
                    else:
                        # Stack the matrix headers
                        worksheet.write(0, c, row0[c], fmt_cc_header)
                        worksheet.write(1, c, row1[c], fmt_gc_header)
                        worksheet.write(2, c, row2[c], fmt_course_header)

                # Set column widths
                worksheet.set_column(0, 0, 22) # Name
                worksheet.set_column(1, 1, 35) # Email
                worksheet.set_column(2, 4, 18) # Location, Title, Manager
                worksheet.set_column(5, cols - 1, 14, pct_format) # Course percentages
                
                # Make the course name row taller to fit text wrap
                worksheet.set_row(2, 60)

                writer.close()
            except ImportError:
                # Safe fallback if xlsxwriter is missing
                fallback_df = pd.DataFrame([row0, row1, row2] + data)
                fallback_df.to_excel(path, index=False, header=False)

            QMessageBox.information(self.ui, "Export Successful", f"Filtered Matrix successfully exported to:\n{path}")
            
        except Exception as exc:
            QMessageBox.critical(self.ui, "Export Error", f"Failed to export matrix:\n{str(exc)}")
        finally:
            QApplication.restoreOverrideCursor()

    def open_mapping_manager(self):
        dialog = CourseMappingDialog(self.current_org_filter, self.ui)
        if dialog.exec_() == QDialog.Accepted or dialog.courses_deleted:
            self.load_matrix_data()

    def setup_filters(self):
        self.ui.combo_matrix_region.clear()
        self.ui.combo_matrix_region.addItems(
            ["All Regions", "Americas", "Europe", "China", "Taiwan",
             "Japan", "Korea", "ISEA", "Worldwide"]
        )
        self.ui.combo_matrix_region.currentTextChanged.connect(self.apply_filters)

        self.ui.combo_matrix_code.clear()
        self.ui.combo_matrix_code.addItems(
            ["All Codes", "Low & Mid Power", "High Power", "Automotive", "Motor Driver"]
        )
        self.ui.combo_matrix_code.currentTextChanged.connect(self.apply_filters)

        self.ui.search_matrix.textChanged.connect(self.apply_filters)

    def apply_filters(self):
        self.proxy.set_filters(
            self.ui.search_matrix.text(),
            self.ui.combo_matrix_region.currentText(),
            self.ui.combo_matrix_code.currentText()
        )

    def on_tab_changed(self, index):
        self.current_org_filter = ["FAE", "Sales", "Unknown"][index]
        self.load_matrix_data()

    def fetch_matrix_data(self):
        with sqlite3.connect(DB_PATH) as conn:
            cursor = conn.cursor()

            ALLOWED_ORGS = {"FAE", "Sales", "Unknown"}
            if self.current_org_filter not in ALLOWED_ORGS:
                raise ValueError(
                    f"Invalid org filter: {self.current_org_filter!r}"
                )

            cursor.execute("SELECT value FROM app_settings WHERE key = 'category_tree'")
            row = cursor.fetchone()
            structure = json.loads(row[0]) if row else {}
            
            cc_order = {cc: i for i, cc in enumerate(structure.keys())}
            
            prefix = self.current_org_filter.lower()
            cursor.execute(f"""
                SELECT course_id, course_name, {prefix}_control_code, {prefix}_graph_category, {prefix}_sort_order
                FROM   courses
                WHERE  course_type IN (?, 'Both')
            """, (self.current_org_filter,))
            raw_courses = cursor.fetchall()
            
            filtered_courses = []
            for cid, cname, cc, gc, sort_order in raw_courses:
                if cc == "Unassigned" or gc == "Unassigned": 
                    continue # Exclude unassigned courses from Matrix View
                    
                cname_lower = cname.lower()
                if self.current_org_filter == "FAE" and "sales" in cname_lower: continue
                if self.current_org_filter == "Sales" and "fae" in cname_lower: continue
                filtered_courses.append((cid, cname, cc, gc, sort_order))
                
            filtered_courses.sort(key=lambda x: (
                cc_order.get(x[2], 999), 
                x[3], 
                x[4], 
                x[1]
            ))
            courses = filtered_courses

            cursor.execute(
                """
                SELECT user_id,
                       first_name || ' ' || last_name,
                       email, location, title, manager,
                       control_codes, region_bucket
                FROM   students
                WHERE  organization = ?
                ORDER BY
                    CASE WHEN region_bucket = 'Worldwide' OR region_bucket IS NULL
                         THEN 1 ELSE 0 END,
                    region_bucket ASC, location ASC, first_name ASC
                """,
                (self.current_org_filter,),
            )
            students = cursor.fetchall()

            cursor.execute(
                """
                SELECT c.user_id, c.course_id, c.percent_complete
                FROM   completions c
                JOIN   students s ON s.user_id = c.user_id
                WHERE  s.organization = ?
                """,
                (self.current_org_filter,),
            )
            completions_map = {}
            for uid, cid, percent in cursor.fetchall():
                uid_norm = normalize_uid(uid)
                completions_map.setdefault(uid_norm, {})[cid] = safe_float(percent)

        return courses, students, completions_map

    def load_matrix_data(self):
        QApplication.setOverrideCursor(Qt.WaitCursor)
        try:
            courses, students, completions_map = self.fetch_matrix_data()
            header_table = self.ui.header_table

            # Crucial fix to resolve "span cannot overlap" bug
            header_table.clearSpans()
            header_table.setUpdatesEnabled(False)
            header_table.clear()

            base_headers = ["Name", "Email", "Location", "Title", "Manager"]
            total_cols   = len(base_headers) + len(courses)

            header_table.setColumnCount(total_cols)
            header_table.setRowCount(3)

            for r in range(3):
                header_table.setVerticalHeaderItem(r, QTableWidgetItem(""))

            for c in range(total_cols):
                header_table.setHorizontalHeaderItem(c, QTableWidgetItem(""))

            col_widths = [180, 240, 210, 210, 150]
            for i in range(len(base_headers)):
                header_table.setColumnWidth(i, col_widths[i])
            for i in range(5, total_cols):
                header_table.setColumnWidth(i, 130)

            # Updated header row heights for readability
            header_table.setRowHeight(0, 30)
            header_table.setRowHeight(1, 30)
            header_table.setRowHeight(2, 90)
            header_table.setFixedHeight(170)

            font_bold = header_table.font()
            font_bold.setBold(True)
            font_small = header_table.font()
            font_small.setPointSize(9)

            for col_idx, text in enumerate(base_headers):
                item = QTableWidgetItem(text)
                item.setTextAlignment(Qt.AlignCenter | Qt.AlignVCenter)
                item.setBackground(QColor("#202025"))
                item.setForeground(QColor("white"))
                item.setFont(font_bold)
                header_table.setItem(0, col_idx, item)
                header_table.setSpan(0, col_idx, 3, 1)

            current_cc, cc_start_col = None, 5
            current_gc, gc_start_col = None, 5

            for i, (course_id, cname, cc, gc, sort_order) in enumerate(courses):
                col_idx = 5 + i
                c_item  = QTableWidgetItem(cname if cname else "Unnamed")
                c_item.setTextAlignment(Qt.AlignCenter | Qt.AlignVCenter)
                c_item.setBackground(QColor("#2a2a30"))
                c_item.setForeground(QColor("white"))
                c_item.setFont(font_small)
                header_table.setItem(2, col_idx, c_item)

                if current_cc != cc:
                    if current_cc is not None and (col_idx - cc_start_col) > 1:
                        header_table.setSpan(0, cc_start_col, 1, col_idx - cc_start_col)
                    current_cc = cc
                    cc_start_col = col_idx
                    cc_item = QTableWidgetItem(cc)
                    cc_item.setTextAlignment(Qt.AlignCenter | Qt.AlignVCenter)
                    cc_item.setBackground(QColor("#4472C4"))
                    cc_item.setForeground(QColor("white"))
                    cc_item.setFont(font_bold)
                    header_table.setItem(0, col_idx, cc_item)

                if current_gc != gc or cc_start_col == col_idx:
                    if current_gc is not None and (col_idx - gc_start_col) > 1:
                        header_table.setSpan(1, gc_start_col, 1, col_idx - gc_start_col)
                    current_gc = gc
                    gc_start_col = col_idx
                    gc_item = QTableWidgetItem(gc)
                    gc_item.setTextAlignment(Qt.AlignCenter | Qt.AlignVCenter)
                    gc_item.setBackground(QColor("#666666"))
                    gc_item.setForeground(QColor("white"))
                    gc_item.setFont(font_bold)
                    header_table.setItem(1, col_idx, gc_item)

            if courses:
                last_col = 5 + len(courses)
                if (last_col - cc_start_col) > 1:
                    header_table.setSpan(0, cc_start_col, 1, last_col - cc_start_col)
                if (last_col - gc_start_col) > 1:
                    header_table.setSpan(1, gc_start_col, 1, last_col - gc_start_col)

            table_data = []
            for student in students:
                user_id, name, email, location, title, manager, student_codes, region_bucket = student
                student_codes_list = (
                    [c.strip() for c in student_codes.split(",")]
                    if student_codes else []
                )
                uid_norm = normalize_uid(user_id)

                row_cells = []
                row_cells.append({"text": name or "N/A", "codes": student_codes or ""})
                row_cells.append({"text": email or "N/A"})
                row_cells.append({"text": location or "N/A", "region": region_bucket or "Worldwide"})
                row_cells.append({"text": title or "N/A"})
                row_cells.append({"text": manager or "N/A"})

                user_completions = completions_map.get(uid_norm, {})
                for course_id, _, course_control_code, _, _ in courses:
                    pct = user_completions.get(course_id, 0.0)

                    if course_control_code not in student_codes_list:
                        row_cells.append({"text": "N/A", "bg": None, "fg": self.model.color_gray})
                    elif pct >= 100.0:
                        row_cells.append({"text": "1.00", "bg": self.model.color_green, "fg": Qt.black})
                    elif pct > 0.0:
                        row_cells.append({"text": "0.01", "bg": self.model.color_yellow, "fg": Qt.black})
                    else:
                        row_cells.append({"text": "0.00", "bg": self.model.color_red, "fg": Qt.white})

                table_data.append(row_cells)

            self.model.update_data(table_data)
            
            for i in range(len(base_headers)):
                self.ui.table_matrix.setColumnWidth(i, col_widths[i])
            for i in range(5, total_cols):
                self.ui.table_matrix.setColumnWidth(i, 130)

            self.apply_filters()
        finally:
            header_table.setUpdatesEnabled(True)
            QApplication.restoreOverrideCursor()
</file>

<file path="components/tab_students.py">
from PyQt5.QtWidgets import (
    QTableWidgetItem, QHeaderView, QApplication, QDialog,
    QComboBox, QCheckBox, QMessageBox, QMenu, QWidgetAction,
    QFileDialog, QLineEdit, QDoubleSpinBox, QTableWidget,
    QTableView, QStyledItemDelegate, QVBoxLayout, QHBoxLayout, QLabel, QPushButton
)
from PyQt5.QtGui import QStandardItemModel, QStandardItem, QColor, QBrush
from PyQt5.QtCore import Qt, QSettings, pyqtSignal, QThread, QAbstractTableModel, QSortFilterProxyModel
from PyQt5 import uic
import pandas as pd
import sqlite3
import os
import sys
import json
import glob
import datetime
import shutil

from database import DB_PATH
from data_manager import (
    DataManager, parse_region, load_dataframe, safe_float, normalize_uid,
    INTERNAL_DOMAINS,
)

def resource_path(relative_path):
    try:
        base_path = sys._MEIPASS
    except Exception:
        base_path = os.path.abspath(".")
    return os.path.join(base_path, relative_path)

# ── Email safety constants ─────────────────────────────────────────────────────

try:
    import bleach
    BLEACH_AVAILABLE = True
except ImportError:
    BLEACH_AVAILABLE = False


# ── Default progress rules (Emojis on the right) ──────────────────────────────
DEFAULT_RULES = [
    {
        "name": "Up-to-Date ✅",
        "metric": "Overall Completion %",
        "operator": "==",
        "value": 100.0,
        "subject": "You’re All Set 👍",
        "body": "Hi [Name],<br><br>Just a quick note to say you’re currently up to date with all required training modules.<br><br>Great job staying on track. This really helps ensure you’re ready to support customers effectively.<br><br>We’ll keep you posted when new modules are released.<br><br>Thanks,<br>Training Team",
    },
    {
        "name": "New Students 👋",
        "metric": "Overall Completion %",
        "operator": "==",
        "value": -1.0, 
        "subject": "Welcome – Getting Started with Your Training",
        "body": "Hi [Name],<br><br>Welcome to the Power Integrations Skilljar Training.<br><br>Skilljar is our company training bank. It is accessible through <a href='https://powertest.skilljar.com'>https://powertest.skilljar.com</a>.<br><br>To get started, you can begin with your assigned modules.<br><br>We recommend starting with the first module and progressing step by step.<br><br>If you have any questions along the way, feel free to reach out.<br><br>Thanks,<br>Training Team",
    },
    {
        "name": "No Action (0%) ⚠️",
        "metric": "Overall Completion %",
        "operator": "==",
        "value": 0.0,
        "subject": "Reminder to Complete Your Training",
        "body": "Hi [Name],<br><br>Just a quick reminder to begin your assigned training modules.<br><br>We noticed there hasn’t been any progress yet. Getting started early will help you complete the content more smoothly.<br><br>You can access your training here:<br><a href='https://powertest.skilljar.com'>https://powertest.skilljar.com</a><br><br>Let me know if you need any support or if there are any blockers.<br><br>Thanks,<br>Training Team",
    },
    {
        "name": "Progressed This Month 🚀",
        "metric": "Monthly Progress %",
        "operator": ">",
        "value": 0.0,
        "subject": "Nice Progress This Month 👏",
        "body": "Hi [Name],<br><br>Nice work on your progress this month.<br><br>Your current completion rate is now at [X%], showing solid improvement. Keep it going to stay on track and complete the remaining modules.<br><br>Next step: continue with your remaining modules here:<br><a href='https://powertest.skilljar.com'>https://powertest.skilljar.com</a><br><br>Thanks,<br>Training Team",
    },
    {
        "name": "Missed New Module 🆕",
        "metric": "Missed New Modules",
        "operator": "==",
        "value": 1.0,
        "subject": "New Module Available – Please Review",
        "body": "Hi [Name],<br><br>A new training module has been released, but it looks like you haven’t accessed it yet.<br><br>You can find it here:<br><a href='https://powertest.skilljar.com'>https://powertest.skilljar.com</a><br><br>We recommend going through it as soon as possible to stay up to date with the latest content.<br><br>Let me know if you have any questions.<br><br>Thanks,<br>Training Team",
    },
    {
        "name": "Stagnant Students ⏳",
        "metric": "Default",
        "operator": "==",
        "value": 0.0,
        "subject": "Quick Check on Your Training Progress",
        "body": "Hi [Name],<br><br>We noticed that your current training progress is at [completion], with no activity recorded from the previous month. If you’re currently busy or facing challenges, that’s totally understandable.<br><br>Let us know if you need help or if there’s anything blocking you from continuing.<br><br>You can resume your training here:<br><a href='https://powertest.skilljar.com'>https://powertest.skilljar.com</a><br><br>Thanks,<br>Training Team",
    },
]

# ── Month Year Selection Dialog ────────────────────────────────────────────────
class MonthYearDialog(QDialog):
    def __init__(self, parent=None):
        super().__init__(parent)
        self.setWindowTitle("Select Data Month")
        self.setStyleSheet("""
            QDialog { background: #202025; color: white; }
            QLabel { color: white; font-weight: bold; }
            QComboBox { background: #1a1a1e; color: white; border: 1px solid #3a3a40; padding: 4px; }
            QPushButton { padding: 6px; border-radius: 4px; font-weight: bold; background: #0085ca; color: white; }
            QPushButton:hover { background: #3c649f; }
        """)
        
        layout = QVBoxLayout(self)
        
        hbox = QHBoxLayout()
        self.month_combo = QComboBox()
        self.month_combo.addItems([
            "January", "February", "March", "April", "May", "June", 
            "July", "August", "September", "October", "November", "December"
        ])
        
        self.year_combo = QComboBox()
        current_year = datetime.datetime.now().year
        self.year_combo.addItems([str(y) for y in range(current_year-2, current_year+3)])
        self.year_combo.setCurrentText(str(current_year))
        
        current_month_idx = datetime.datetime.now().month - 1
        self.month_combo.setCurrentIndex(current_month_idx)
        
        hbox.addWidget(QLabel("Month:"))
        hbox.addWidget(self.month_combo)
        hbox.addWidget(QLabel("Year:"))
        hbox.addWidget(self.year_combo)
        layout.addLayout(hbox)
        
        btn_layout = QHBoxLayout()
        self.btn_ok = QPushButton("OK")
        self.btn_cancel = QPushButton("Cancel")
        self.btn_ok.clicked.connect(self.accept)
        self.btn_cancel.clicked.connect(self.reject)
        btn_layout.addWidget(self.btn_ok)
        btn_layout.addWidget(self.btn_cancel)
        layout.addLayout(btn_layout)

    def get_selection(self):
        return self.month_combo.currentIndex() + 1, int(self.year_combo.currentText()), self.month_combo.currentText()


# ── Background worker for progress calculation ─────────────────────────────────
class ProgressWorker(QThread):
    done = pyqtSignal(dict)
    error = pyqtSignal(str)

    def __init__(self, component, current_year, current_month):
        super().__init__()
        self._component = component
        self._current_year = current_year
        self._current_month = current_month

    def run(self):
        try:
            result = self._component.calculate_progress(self._current_year, self._current_month)
            self.done.emit(result)
        except Exception as exc:
            self.error.emit(str(exc))


# ── Template editor dialog ─────────────────────────────────────────────────────
class TemplateEditorDialog(QDialog):
    def __init__(self, rules, parent=None):
        super().__init__(parent)
        uic.loadUi(resource_path("dialog_email_templates.ui"), self)
        self.rules = rules

        self.combo_category.addItems([r["name"] for r in self.rules])
        self.combo_category.currentTextChanged.connect(self.load_template)

        self.edit_subject.textChanged.connect(self.save_current_template)
        self.edit_body.textChanged.connect(self.save_current_template)

        if self.rules:
            self.load_template(self.rules[0]["name"])

    def load_template(self, category_name):
        self.edit_subject.blockSignals(True)
        self.edit_body.blockSignals(True)

        for rule in self.rules:
            if rule["name"] == category_name:
                self.edit_subject.setText(rule.get("subject", "Power Integrations Training Update"))
                self.edit_body.setHtml(rule.get("body", "Hi [Name],<br><br>Please check your training portal."))
                break

        self.edit_subject.blockSignals(False)
        self.edit_body.blockSignals(False)

    def save_current_template(self):
        category_name = self.combo_category.currentText()
        for rule in self.rules:
            if rule["name"] == category_name:
                rule["subject"] = self.edit_subject.text()
                rule["body"]    = self.edit_body.toHtml()
                break


# ── Progress logic dialog ──────────────────────────────────────────────────────
class ProgressLogicDialog(QDialog):
    def __init__(self, parent=None):
        super().__init__(parent)
        uic.loadUi(resource_path("dialog_progress_logic.ui"), self)

        self.settings = QSettings("PowerIntegrations", "PowerTrack")
        # Load v3 rules to ensure new emoji rules apply cleanly
        self.rules = json.loads(self.settings.value("progress_rules_v3", json.dumps(DEFAULT_RULES)))

        for r in self.rules:
            if "subject" not in r: r["subject"] = "Power Integrations Training Update"
            if "body" not in r: r["body"] = "Hi [Name],<br><br>Please check your training portal."

        self.table_rules.horizontalHeader().setSectionResizeMode(QHeaderView.Stretch)
        self.table_rules.setSelectionBehavior(QTableWidget.SelectRows)

        self.btn_add.clicked.connect(self.add_rule)
        self.btn_delete.clicked.connect(self.delete_rule)
        self.btn_up.clicked.connect(self.move_up)
        self.btn_down.clicked.connect(self.move_down)
        self.btn_edit_templates.clicked.connect(self.open_template_editor)
        self.buttonBox.accepted.connect(self.save_rules)

        self.populate_table()

    def populate_table(self):
        self.table_rules.setRowCount(0)
        for rule in self.rules:
            self.add_rule_row(rule)

    def add_rule_row(self, rule=None):
        if rule is None:
            rule = {"name": "New Category", "metric": "Monthly Progress %", "operator": ">=", "value": 20.0, "subject": "Training Update", "body": "Please review your training."}

        row = self.table_rules.rowCount()
        self.table_rules.insertRow(row)

        name_edit = QLineEdit(rule["name"])
        name_edit.setStyleSheet("background: transparent; color: white; border: none; padding: 5px;")
        self.table_rules.setCellWidget(row, 0, name_edit)

        metric_combo = QComboBox()
        metric_combo.addItems(["Overall Completion %", "Monthly Progress %", "Missed New Modules", "Default"])
        metric_combo.setCurrentText(rule["metric"])
        self.table_rules.setCellWidget(row, 1, metric_combo)

        op_combo = QComboBox()
        op_combo.addItems([">", ">=", "<", "<=", "=="])
        op_combo.setCurrentText(rule["operator"])
        self.table_rules.setCellWidget(row, 2, op_combo)

        val_spin = QDoubleSpinBox()
        val_spin.setRange(-100, 100)
        val_spin.setValue(float(rule["value"]))
        self.table_rules.setCellWidget(row, 3, val_spin)

    def add_rule(self): self.add_rule_row()
    def delete_rule(self):
        row = self.table_rules.currentRow()
        if row >= 0: self.table_rules.removeRow(row)

    def move_up(self):
        row = self.table_rules.currentRow()
        if row > 0:
            self.sync_ui_to_rules()
            self.rules[row], self.rules[row - 1] = self.rules[row - 1], self.rules[row]
            self.populate_table()
            self.table_rules.selectRow(row - 1)

    def move_down(self):
        row = self.table_rules.currentRow()
        if row >= 0 and row < self.table_rules.rowCount() - 1:
            self.sync_ui_to_rules()
            self.rules[row], self.rules[row + 1] = self.rules[row + 1], self.rules[row]
            self.populate_table()
            self.table_rules.selectRow(row + 1)

    def get_row_data(self, row):
        current_name = self.table_rules.cellWidget(row, 0).text()
        subj = "Power Integrations Training Update"
        body = "Hi [Name],<br><br>Please check your training portal."
        for r in self.rules:
            if r["name"] == current_name:
                subj = r.get("subject", subj)
                body = r.get("body", body)
                break
        return {"name": current_name, "metric": self.table_rules.cellWidget(row, 1).currentText(), "operator": self.table_rules.cellWidget(row, 2).currentText(), "value": self.table_rules.cellWidget(row, 3).value(), "subject": subj, "body": body}

    def sync_ui_to_rules(self):
        self.rules = [self.get_row_data(i) for i in range(self.table_rules.rowCount())]

    def open_template_editor(self):
        self.sync_ui_to_rules()
        editor = TemplateEditorDialog(self.rules, self)
        editor.exec_()

    def save_rules(self):
        self.sync_ui_to_rules()
        self.settings.setValue("progress_rules_v3", json.dumps(self.rules))


# ── Multi-select combobox for Delegate ─────────────────────────────────────────
class CheckableComboBox(QComboBox):
    def __init__(self, parent=None):
        super().__init__(parent)
        self.setModel(QStandardItemModel(self))
        self.setEditable(True)
        self.lineEdit().setReadOnly(True)
        self.lineEdit().setAlignment(Qt.AlignCenter)
        self.view().pressed.connect(self.handle_item_pressed)

    def handle_item_pressed(self, index):
        item = self.model().itemFromIndex(index)
        item.setCheckState(Qt.Unchecked if item.checkState() == Qt.Checked else Qt.Checked)
        self.update_text()

    def set_items(self, items, checked_items):
        self.clear()
        for text in items:
            item = QStandardItem(text)
            item.setFlags(Qt.ItemIsEnabled | Qt.ItemIsUserCheckable)
            item.setData(Qt.Checked if text in checked_items else Qt.Unchecked, Qt.CheckStateRole)
            self.model().appendRow(item)
        self.update_text()

    def update_text(self):
        checked = [self.model().item(i).text() for i in range(self.model().rowCount()) if self.model().item(i).checkState() == Qt.Checked]
        text = ", ".join(checked)
        self.lineEdit().setText(text)
        return text

# ── Delegates for QTableView ───────────────────────────────────────────────────
class OrgDelegate(QStyledItemDelegate):
    def createEditor(self, parent, option, index):
        combo = QComboBox(parent)
        combo.addItems(["FAE", "Sales", "Unknown"])
        return combo

    def setEditorData(self, editor, index):
        value = index.model().data(index, Qt.EditRole)
        idx = editor.findText(value)
        if idx >= 0: editor.setCurrentIndex(idx)

    def setModelData(self, editor, model, index):
        model.setData(index, editor.currentText(), Qt.EditRole)

class ControlCodeDelegate(QStyledItemDelegate):
    def createEditor(self, parent, option, index):
        return CheckableComboBox(parent)

    def setEditorData(self, editor, index):
        val_str = index.model().data(index, Qt.EditRole)
        checked_codes = [c.strip() for c in val_str.split(",")] if val_str and val_str != "N/A" else []
        
        with sqlite3.connect(DB_PATH) as conn:
            cursor = conn.cursor()
            cursor.execute("SELECT value FROM app_settings WHERE key = 'category_tree'")
            row = cursor.fetchone()
            structure = json.loads(row[0]) if row else {}
            
        editor.set_items(list(structure.keys()), checked_codes)

    def setModelData(self, editor, model, index):
        model.setData(index, editor.lineEdit().text(), Qt.EditRole)

# ── Models for UI Virtualization ───────────────────────────────────────────────
class StudentTableModel(QAbstractTableModel):
    def __init__(self, parent=None):
        super().__init__(parent)
        self.headers = ["Name", "Email", "Location", "Title", "Manager", "Organization", "Control Code", "Avg. Completion", "Progress"]
        self._data = []

    def rowCount(self, parent=None): return len(self._data)
    def columnCount(self, parent=None): return len(self.headers)

    def data(self, index, role=Qt.DisplayRole):
        if not index.isValid(): return None
        row, col = index.row(), index.column()
        
        if role == Qt.DisplayRole or role == Qt.EditRole: return str(self._data[row][col])
        elif role == Qt.TextAlignmentRole: return Qt.AlignCenter | Qt.AlignVCenter if col >= 1 else Qt.AlignLeft | Qt.AlignVCenter
        elif role == Qt.UserRole: 
            if col == 0: return self._data[row][9]  
            if col == 2: return self._data[row][10] 
            if col == 8: return self._data[row][8]  
        return None

    def setData(self, index, value, role=Qt.EditRole):
        if index.isValid() and role == Qt.EditRole:
            row, col = index.row(), index.column()
            old_val = str(self._data[row][col])
            new_val = str(value).strip()
            
            if old_val == new_val: return False
                
            self._data[row][col] = new_val
            user_id = self._data[row][9]
            ALLOWED_SINGLE_COLS = {1: "email", 3: "title", 4: "manager"}

            with sqlite3.connect(DB_PATH) as conn:
                cursor = conn.cursor()
                if col == 0:
                    parts = new_val.split(" ", 1)
                    cursor.execute("UPDATE students SET first_name=?, last_name=? WHERE user_id=?", (parts[0], parts[1] if len(parts) > 1 else "", user_id))
                elif col == 2:
                    new_region = parse_region(new_val)
                    self._data[row][10] = new_region
                    cursor.execute("UPDATE students SET location=?, region_bucket=? WHERE user_id=?", (new_val, new_region, user_id))
                elif col == 5:
                    cursor.execute("UPDATE students SET organization=? WHERE user_id=?", (new_val, user_id))
                elif col == 6:
                    cursor.execute("UPDATE students SET control_codes=? WHERE user_id=?", (new_val, user_id))
                elif col in ALLOWED_SINGLE_COLS:
                    col_name = ALLOWED_SINGLE_COLS[col]
                    cursor.execute(f"UPDATE students SET {col_name}=? WHERE user_id=?", (new_val, user_id))
                    
            self.dataChanged.emit(index, index, [Qt.DisplayRole, Qt.EditRole])
            return True
        return False

    def flags(self, index):
        if not index.isValid(): return Qt.NoItemFlags
        flags = Qt.ItemIsSelectable | Qt.ItemIsEnabled
        if index.column() not in (7, 8): flags |= Qt.ItemIsEditable
        return flags

    def headerData(self, section, orientation, role=Qt.DisplayRole):
        if role == Qt.DisplayRole:
            if orientation == Qt.Horizontal: 
                return self.headers[section]
            elif orientation == Qt.Vertical:
                return str(section + 1)
        return None

    def update_data(self, new_data):
        self.beginResetModel()
        self._data = new_data
        self.endResetModel()

class StudentFilterProxyModel(QSortFilterProxyModel):
    def __init__(self, parent=None):
        super().__init__(parent)
        self.search_text = ""
        self.target_region = "All Regions"
        self.target_progress = "All Status"

    def set_filters(self, search, region, progress):
        self.search_text = search.lower()
        self.target_region = region
        self.target_progress = progress
        self.invalidateFilter()

    def filterAcceptsRow(self, source_row, source_parent):
        model = self.sourceModel()
        name = model.data(model.index(source_row, 0, source_parent)).lower()
        email = model.data(model.index(source_row, 1, source_parent)).lower()
        region = model.data(model.index(source_row, 2, source_parent), Qt.UserRole)
        progress = model.data(model.index(source_row, 8, source_parent), Qt.UserRole)

        match_search = (self.search_text in name) or (self.search_text in email)
        match_region = (self.target_region == "All Regions" or region == self.target_region)
        match_progress = (self.target_progress == "All Status" or progress == self.target_progress)
        return match_search and match_region and match_progress

# ── Main student tab component ─────────────────────────────────────────────────
class StudentTabComponent:
    def __init__(self, ui_window):
        self.ui = ui_window
        self.is_loading = True
        self.current_org_filter = "FAE"
        self._progress_worker = None

        self.settings = QSettings("PowerIntegrations", "PowerTrack")
        
        self.current_month = int(self.settings.value("current_month", 0))
        self.current_year = int(self.settings.value("current_year", 0))
        self.current_month_name = self.settings.value("current_month_name", "None")

        try:
            self.progress_dict = json.loads(self.settings.value("progress_dict", "{}"))
        except Exception:
            self.progress_dict = {}
            
        try:
            DataManager().auto_import_master_lists()
        except Exception as e:
            print(f"[WARN] Failed to auto-import master lists: {e}")

        self.setup_ui_bindings()
        self.setup_table()
        self.setup_filters()
        self.update_month_label()
        self.load_students_to_table()

    def setup_ui_bindings(self):
        self.ui.tabBar_students.addTab("FAE")
        self.ui.tabBar_students.addTab("Sales")
        self.ui.tabBar_students.addTab("Unknown")
        self.ui.tabBar_students.currentChanged.connect(self.on_tab_changed)

        self.ui.btn_import_monthly.clicked.connect(self.import_monthly_data)
        self.ui.btn_export_archive.clicked.connect(self.export_month_archive)

        if hasattr(self.ui, "btn_edit_progress"):
            self.ui.btn_edit_progress.clicked.connect(self.open_progress_logic_editor)
        if hasattr(self.ui, "btn_email_students"):
            self.ui.btn_email_students.clicked.connect(self.draft_emails)

    def setup_table(self):
        self.table_view = self.ui.table_students

        self.model = StudentTableModel(self.ui)
        self.proxy = StudentFilterProxyModel(self.ui)
        self.proxy.setSourceModel(self.model)
        self.table_view.setModel(self.proxy)

        self.table_view.setItemDelegateForColumn(5, OrgDelegate(self.ui))
        self.table_view.setItemDelegateForColumn(6, ControlCodeDelegate(self.ui))

        header = self.table_view.horizontalHeader()
        header.setSectionResizeMode(QHeaderView.Interactive)
        header.setContextMenuPolicy(Qt.CustomContextMenu)
        header.customContextMenuRequested.connect(self.show_column_menu)

        # Increase row height to accommodate bigger fonts gracefully
        self.table_view.verticalHeader().setDefaultSectionSize(40)
        self.table_view.verticalHeader().setMinimumWidth(30)
        
        for col, width in enumerate([180, 200, 180, 180, 160, 100, 200, 150]):
            self.table_view.setColumnWidth(col, width)
        header.setStretchLastSection(True)
        self.model.dataChanged.connect(self.on_model_data_changed)

    def on_model_data_changed(self, top_left, bottom_right, roles):
        if self.is_loading: return
        col = top_left.column()
        if col == 5: 
            self.load_students_to_table()
        elif col == 6: 
            QApplication.setOverrideCursor(Qt.WaitCursor)
            self._start_progress_worker()

    def setup_filters(self):
        self.ui.search_student.textChanged.connect(self.apply_filters)
        self.ui.combo_region.clear()
        self.ui.combo_region.addItems(["All Regions", "Americas", "Europe", "China", "Taiwan", "Japan", "Korea", "ISEA", "Worldwide"])
        self.ui.combo_region.currentTextChanged.connect(self.apply_filters)

        rules = json.loads(self.settings.value("progress_rules_v3", json.dumps(DEFAULT_RULES)))
        unique_categories = list(dict.fromkeys(r["name"] for r in rules))
        try: self.ui.combo_progress.currentTextChanged.disconnect(self.apply_filters)
        except TypeError: pass

        self.ui.combo_progress.clear()
        self.ui.combo_progress.addItem("All Status")
        self.ui.combo_progress.addItems(unique_categories)
        self.ui.combo_progress.addItem("N/A")
        self.ui.combo_progress.currentTextChanged.connect(self.apply_filters)
        self.ui.btn_clear_filters.clicked.connect(self.clear_filters)

    def apply_filters(self):
        self.proxy.set_filters(self.ui.search_student.text(), self.ui.combo_region.currentText(), self.ui.combo_progress.currentText())
        self.ui.lbl_showing.setText(f"Showing {self.proxy.rowCount()} of {self.model.rowCount()} students")

    def clear_filters(self):
        self.ui.search_student.clear()
        self.ui.combo_region.setCurrentIndex(0)
        self.ui.combo_progress.setCurrentIndex(0)

    def show_column_menu(self, pos):
        menu  = QMenu(self.ui)
        menu.setStyleSheet("QMenu { background: #202025; color: white; border: 1px solid #3a3a40; }")
        for c in range(self.model.columnCount()):
            action = QWidgetAction(menu)
            chk = QCheckBox(self.model.headerData(c, Qt.Horizontal))
            chk.setStyleSheet("QCheckBox { padding: 5px; background: transparent; color: white; } QCheckBox:hover { background: #0085ca; }")
            chk.setChecked(not self.table_view.isColumnHidden(c))
            chk.toggled.connect(lambda checked, col=c: self.table_view.setColumnHidden(col, not checked))
            action.setDefaultWidget(chk)
            menu.addAction(action)
        menu.exec_(self.table_view.horizontalHeader().viewport().mapToGlobal(pos))

    def update_month_label(self):
        if self.current_month == 0:
            self.ui.lbl_current_month.setText("Current Data View: None (Please Import Data)")
        else:
            self.ui.lbl_current_month.setText(f"Current Data View: {self.current_month_name} {self.current_year}")

    def import_monthly_data(self):
        dialog = MonthYearDialog(self.ui)
        if dialog.exec_() != QDialog.Accepted: return
        month_idx, year, month_name = dialog.get_selection()
        
        if getattr(sys, 'frozen', False):
            base_dir = os.path.dirname(sys.executable)
        else:
            base_dir = os.path.dirname(os.path.abspath(__file__))
            if os.path.basename(base_dir) == 'components':
                base_dir = os.path.dirname(base_dir)
                
        short_month = month_name[:3] 
        folder_name = f"{year}_{month_idx:02d}_{short_month}"
        raw_data_dir = os.path.join(base_dir, "monthly_raw_data", folder_name)
        
        has_files = False
        if not os.path.exists(raw_data_dir):
            os.makedirs(raw_data_dir)
        else:
            files = [f for f in os.listdir(raw_data_dir) if f.lower().endswith(('.csv', '.xlsx', '.xls'))]
            has_files = len(files) > 0
            
        if not has_files:
            QMessageBox.information(
                self.ui, "Folder Created / Empty", 
                f"The expected raw data folder is empty or was just created:\n\n{raw_data_dir}\n\nPlease place your Skilljar export files in this folder and click Import again."
            )
            try:
                os.startfile(raw_data_dir)
            except Exception:
                pass
            return

        QApplication.setOverrideCursor(Qt.WaitCursor)
        try:
            self.current_month = month_idx
            self.current_year = year
            self.current_month_name = month_name
            self.settings.setValue("current_month", self.current_month)
            self.settings.setValue("current_year", self.current_year)
            self.settings.setValue("current_month_name", self.current_month_name)
            self.update_month_label()

            dm = DataManager()
            dm.auto_import_master_lists()
            
            student_csv  = os.path.join(raw_data_dir, "students.csv")
            student_xlsx = os.path.join(raw_data_dir, "students.xlsx")
            if os.path.exists(student_csv): dm.import_students(student_csv)
            elif os.path.exists(student_xlsx): dm.import_students(student_xlsx)

            dm.import_skilljar_metrics(raw_data_dir)
            dm.import_course_files(raw_data_dir)
            dm.import_control_codes(raw_data_dir)
            
            dm.create_monthly_snapshot(self.current_year, self.current_month)

            self._start_progress_worker()
        except Exception as exc:
            QApplication.restoreOverrideCursor()
            QMessageBox.critical(self.ui, "Ingestion Error", str(exc))

    def export_month_archive(self):
        filename = f"Filtered_Students_{self.current_month_name}_{self.current_year}.xlsx"
        path, _ = QFileDialog.getSaveFileName(self.ui, "Export Filtered Students", filename, "Excel Files (*.xlsx)")
        if not path: return

        QApplication.setOverrideCursor(Qt.WaitCursor)
        try:
            # 1. Pull data ONLY from the currently filtered view (self.proxy)
            export_data = []
            for row in range(self.proxy.rowCount()):
                src_row = self.proxy.mapToSource(self.proxy.index(row, 0)).row()
                
                # Extract clean percentage float for Excel formatting
                raw_pct = self.model._data[src_row][7]
                try:
                    clean_pct = float(raw_pct.replace('%', '')) / 100.0 if raw_pct != "N/A" else raw_pct
                except:
                    clean_pct = raw_pct

                export_data.append({
                    "Name": self.model._data[src_row][0],
                    "Email": self.model._data[src_row][1],
                    "Location": self.model._data[src_row][2],
                    "Title": self.model._data[src_row][3],
                    "Manager": self.model._data[src_row][4],
                    "Organization": self.model._data[src_row][5],
                    "Control Code": self.model._data[src_row][6],
                    "Avg. Completion": clean_pct,
                    "Progress": self.model._data[src_row][8]
                })

            df = pd.DataFrame(export_data)

            # 2. Apply the exact tab_analytics formatting (xlsxwriter)
            try:
                writer = pd.ExcelWriter(path, engine="xlsxwriter")
                df.to_excel(writer, sheet_name="Students", index=False)
                worksheet = writer.sheets["Students"]
                
                # Format column widths
                worksheet.set_column(0, 0, 22) # Name
                worksheet.set_column(1, 1, 35) # Email
                worksheet.set_column(2, 6, 18) # Location, Title, Org, etc.
                worksheet.set_column(8, 8, 30) # Progress Category
                
                # Format percentage column specifically
                pct_format = writer.book.add_format({"num_format": "0.00%"})
                worksheet.set_column(7, 7, 18, pct_format) 
                
                # Add stylish Header formatting
                header_format = writer.book.add_format({'bold': True, 'bg_color': '#202025', 'font_color': 'white', 'border': 1})
                for col_num, value in enumerate(df.columns.values):
                    worksheet.write(0, col_num, value, header_format)

                writer.close()
            except ImportError:
                df.to_excel(path, index=False)

            # 3. Keep the Database Backup Logic intact
            if getattr(sys, 'frozen', False):
                base_dir = os.path.dirname(sys.executable)
            else:
                base_dir = os.path.dirname(os.path.abspath(__file__))
                if os.path.basename(base_dir) == 'components':
                    base_dir = os.path.dirname(base_dir)
            
            short_month = self.current_month_name[:3]
            db_backup_dir = os.path.join(base_dir, "archives_and_exports", "Database_Backups")
            os.makedirs(db_backup_dir, exist_ok=True)
            db_backup_path = os.path.join(db_backup_dir, f"powertrack_backup_{self.current_year}_{self.current_month:02d}_{short_month}.db")
            shutil.copy2(DB_PATH, db_backup_path)

            QMessageBox.information(self.ui, "Export Successful", f"Filtered View saved to:\n{path}\n\nA raw database backup was also created in:\narchives_and_exports/Database_Backups/")
        except Exception as exc:
            QMessageBox.critical(self.ui, "Export Error", f"Failed to export:\n{exc}")
        finally:
            QApplication.restoreOverrideCursor()

    def evaluate_rule(self, stats, rule):
        if rule["metric"] == "Default": return True
        val, target, op = stats.get(rule["metric"], 0.0), float(rule["value"]), rule["operator"]
        if op == ">":  return val >  target
        if op == "<":  return val <  target
        if op == ">=": return val >= target
        if op == "<=": return val <= target
        if op == "==": return val == target
        return False

    def calculate_progress(self, current_year, current_month):
        prev_month = current_month - 1
        prev_year = current_year
        if prev_month == 0:
            prev_month = 12
            prev_year -= 1

        rules = json.loads(self.settings.value("progress_rules_v3", json.dumps(DEFAULT_RULES)))

        with sqlite3.connect(DB_PATH) as conn:
            cursor = conn.cursor()
            cursor.execute("SELECT user_id, organization, control_codes FROM students")
            student_data = cursor.fetchall()
            
            cursor.execute("SELECT course_id, fae_control_code, sales_control_code, unknown_control_code FROM courses")
            course_data = cursor.fetchall()
            
            cursor.execute("SELECT user_id, course_id, percent_complete FROM completions")
            current_comps = {}
            for uid, cid, pct in cursor.fetchall():
                current_comps.setdefault(normalize_uid(uid), {})[str(cid)] = safe_float(pct)
                
            cursor.execute("SELECT user_id, course_id, percent_complete FROM completions_history WHERE year=? AND month=?", (prev_year, prev_month))
            prev_comps, prev_all_courses = {}, set()
            for uid, cid, pct in cursor.fetchall():
                prev_comps.setdefault(normalize_uid(uid), {})[str(cid)] = safe_float(pct)
                prev_all_courses.add(str(cid))

        progress_dict = {}
        for uid, org, control_codes in student_data:
            uid_str = normalize_uid(uid)
            my_required_codes = [c.strip() for c in str(control_codes).split(",")] if control_codes else []
            
            if not my_required_codes or my_required_codes == [""]:
                progress_dict[uid_str] = "N/A"
                continue

            curr_courses = current_comps.get(uid_str, {})
            prev_courses = prev_comps.get(uid_str, {})
            
            idx = 1 if org == 'FAE' else 2 if org == 'Sales' else 3
            my_req_cids = [str(cid) for cid, *ccs in course_data if ccs[idx-1] in my_required_codes]

            if not my_req_cids:
                progress_dict[uid_str] = "N/A"
                continue

            overall_pct = sum(curr_courses.get(c, 0.0) for c in my_req_cids) / len(my_req_cids)
            base_overall_pct = sum(prev_courses.get(c, 0.0) for c in my_req_cids) / len(my_req_cids)

            missed_new = 0.0
            for c in my_req_cids:
                if curr_courses.get(c, 0.0) == 0.0:
                    if c not in prev_all_courses:
                        missed_new = 1.0
                        break

            stats = {"Overall Completion %": overall_pct, "Monthly Progress %": overall_pct - base_overall_pct, "Missed New Modules": missed_new, "Default": 0.0}
            
            assigned_category = "Unknown"
            for rule in rules:
                if self.evaluate_rule(stats, rule):
                    assigned_category = rule["name"]
                    break
            progress_dict[uid_str] = assigned_category

        return progress_dict

    def _start_progress_worker(self):
        self._progress_worker = ProgressWorker(self, self.current_year, self.current_month)
        self._progress_worker.done.connect(self._on_progress_done)
        self._progress_worker.error.connect(self._on_progress_error)
        self._progress_worker.start()

    def _on_progress_done(self, result):
        QApplication.restoreOverrideCursor()
        self.progress_dict = result
        self.settings.setValue("progress_dict", json.dumps(self.progress_dict))
        self.setup_filters()
        self.load_students_to_table()

    def _on_progress_error(self, message):
        QApplication.restoreOverrideCursor()
        QMessageBox.critical(self.ui, "Progress Calculation Error", message)

    def open_progress_logic_editor(self):
        dialog = ProgressLogicDialog(self.ui)
        if dialog.exec_() == QDialog.Accepted:
            QApplication.setOverrideCursor(Qt.WaitCursor)
            self._start_progress_worker()

    def draft_emails(self):
        try: 
            import win32com.client as win32
        except ImportError:
            QMessageBox.warning(self.ui, "Missing Library", "Outlook integration requires the 'pywin32' library.\n\nRun:  pip install pywin32")
            return
            
        if not BLEACH_AVAILABLE:
            QMessageBox.warning(self.ui, "Security Warning", "The HTML sanitization library 'bleach' is not installed. Sending HTML emails without it is disabled to prevent security vulnerabilities.\n\nRun: pip install bleach")
            return

        category_students = {}
        for row in range(self.proxy.rowCount()):
            src_row = self.proxy.mapToSource(self.proxy.index(row, 0)).row()
            name = self.model._data[src_row][0]
            email = self.model._data[src_row][1]
            completion = self.model._data[src_row][7]
            progress = self.model._data[src_row][8]
            
            if email and email != "N/A" and "@" in email:
                category_students.setdefault(progress, []).append({
                    "name": name,
                    "email": email,
                    "completion": completion
                })

        if not category_students:
            QMessageBox.information(self.ui, "No Students", "No valid emails found in the current filtered view.")
            return

        total_recipients = sum(len(v) for v in category_students.values())
        if QMessageBox.question(self.ui, "Confirm Email Drafts", f"This will generate {total_recipients} INDIVIDUAL email draft(s) in your Outlook.\n\nThis method allows placeholders like [Name] and [X%] to be personalized for each student.\n\nContinue?", QMessageBox.Yes | QMessageBox.No, QMessageBox.No) != QMessageBox.Yes: return

        QApplication.setOverrideCursor(Qt.WaitCursor)
        try: 
            outlook = win32.Dispatch("outlook.application")
            
            rules = json.loads(self.settings.value("progress_rules_v3", json.dumps(DEFAULT_RULES)))
            templates = {r["name"]: {"subject": r.get("subject", ""), "body": r.get("body", "")} for r in rules}
            default_template = {"subject": "Power Integrations Training Update", "body": "Hi [Name],<br><br>Please check your Power Integrations training portal for updates."}

            allowed_tags = ['b', 'i', 'u', 'br', 'a', 'p', 'span', 'strong', 'em', 'ul', 'ol', 'li']
            allowed_attrs = {'*': ['style'], 'a': ['href', 'title']}
            allowed_styles = ['color', 'background-color', 'font-size', 'font-family', 'text-align']

            drafts_created = 0

            for category, students in category_students.items():
                tmpl = templates.get(category, default_template)
                
                for student in students:
                    mail = outlook.CreateItem(0)
                    mail.To = student["email"]
                    
                    # Extract first name
                    first_name = student["name"].split()[0] if student["name"] and student["name"] != "N/A" else "Team"
                    
                    # Replace placeholders
                    subject = tmpl["subject"].replace("[Name]", first_name).replace("[X%]", student["completion"]).replace("[completion]", student["completion"])
                    raw_body = tmpl["body"].replace("[Name]", first_name).replace("[X%]", student["completion"]).replace("[completion]", student["completion"])
                    
                    mail.Subject = subject
                    mail.HTMLBody = bleach.clean(raw_body, tags=allowed_tags, attributes=allowed_attrs, styles=allowed_styles)
                    
                    # Save securely to the Outlook 'Drafts' folder instead of flashing 50 windows open
                    mail.Save() 
                    drafts_created += 1

            QApplication.restoreOverrideCursor()
            QMessageBox.information(self.ui, "Success", f"Successfully created {drafts_created} individual draft(s).\n\nPlease open your Outlook 'Drafts' folder to review and send them.")

        except Exception as exc:
            QApplication.restoreOverrideCursor()
            QMessageBox.critical(self.ui, "Outlook Error", f"Could not create drafts. Is Outlook installed and running?\n\nError: {exc}")

    def on_tab_changed(self, index):
        self.current_org_filter = ["FAE", "Sales", "Unknown"][index]
        self.load_students_to_table()

    def get_all_students(self):
        with sqlite3.connect(DB_PATH) as conn:
            cursor = conn.cursor()
            cursor.execute(
                """
                SELECT user_id, first_name || ' ' || last_name AS name, email, location, organization, 
                       title, manager, region_bucket, control_codes, signed_up
                FROM students WHERE organization = ?
                ORDER BY CASE WHEN region_bucket = 'Worldwide' OR region_bucket IS NULL THEN 1 ELSE 0 END,
                         region_bucket ASC, location ASC, first_name ASC
                """, [self.current_org_filter]
            )
            return cursor.fetchall()

    def load_students_to_table(self):
        self.is_loading = True
        try: self.progress_dict = json.loads(self.settings.value("progress_dict", "{}"))
        except Exception: self.progress_dict = {}

        all_students = self.get_all_students()

        with sqlite3.connect(DB_PATH) as conn:
            cursor = conn.cursor()
            prefix = self.current_org_filter.lower()
            cursor.execute(f"SELECT course_id, {prefix}_control_code FROM courses")
            course_map = {str(r[0]): str(r[1]) for r in cursor.fetchall()}

            cursor.execute("SELECT c.user_id, c.course_id, c.percent_complete FROM completions c JOIN students s ON s.user_id = c.user_id WHERE s.organization = ?", (self.current_org_filter,))
            completions_map = {}
            for uid, cid, pct in cursor.fetchall(): completions_map.setdefault(normalize_uid(uid), {})[str(cid)] = safe_float(pct)

        table_data = []
        for student_data in all_students:
            user_id, name, email, location, organization, title, manager, region_bucket, control_codes, signed_up = student_data
            uid_str = normalize_uid(user_id)
            my_codes = [c.strip() for c in str(control_codes).split(",")] if control_codes and str(control_codes).lower() not in ("nan", "n/a", "none", "") else []
            my_req_courses = [cid for cid, cc in course_map.items() if cc in my_codes]

            if not my_req_courses: avg_comp_str = "N/A"
            else:
                total_pct = sum(completions_map.get(uid_str, {}).get(cid, 0.0) for cid in my_req_courses)
                avg_comp_str = f"{(total_pct / len(my_req_courses)):.1f}%"

            table_data.append([
                name or "N/A", email or "N/A", location or "N/A", title or "N/A", manager or "N/A",
                organization or "Unknown", control_codes or "N/A", avg_comp_str, self.progress_dict.get(uid_str, "N/A"),
                user_id, region_bucket, signed_up
            ]) 

        if self.current_org_filter == "Unknown":
            def parse_date(date_str):
                if not date_str or str(date_str).lower() in ("n/a", "nan", "none"): return datetime.datetime.min
                try:
                    return pd.to_datetime(str(date_str)).to_pydatetime()
                except:
                    return datetime.datetime.min
            table_data.sort(key=lambda x: parse_date(x[11]), reverse=True)

        self.model.update_data(table_data)
        self.update_tab_counts(all_students)
        self.apply_filters()
        self.is_loading = False

    def update_tab_counts(self, all_students):
        total = len(all_students)
        internal = sum(1 for s in all_students if any(d in str(s[2]).lower() for d in INTERNAL_DOMAINS))
        self.ui.lbl_total.setText(f"<b>Total:</b> {total}")
        self.ui.lbl_internal.setText(f"Internal: {internal}")
        self.ui.lbl_external.setText(f"External: {total - internal}")
</file>

</files>
